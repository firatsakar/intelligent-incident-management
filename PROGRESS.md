# IIM — Roadmap & Progress

> **Roadmap pozisyonunun tek doğru kaynağı.** Claude Code her görevin başında bunu
> okur ve iş ilerledikçe günceller (bkz. `CLAUDE.md` task lifecycle). Konuşma
> Türkçe; yapısal etiketler (Adım/Parça) Jira ile birebir eşleşsin diye korunur.

## Durum lejantı
- `[x]` **Yapıldı** — bitti, build-doğrulandı (tarih + Jira key ekle)
- `[~]` **Devam ediyor** — üzerinde çalışılıyor (Jira key ekle)
- `[ ]` **Yapılacak** — planlı / sırada
- `[!]` **Yapılmadı** — atlandı veya ertelendi (tek satır gerekçe ekle)

## Son durum
- **Son tamamlanan:** Adım 20 — Dashboard, analitik ve canlı geri bildirim (`IIM-54`, 2026-09-22) — 7 parça, 3 toplama ucu, 4 yeni ekran, soket kapsamı tamamlandı, 285 test
- **Son tamamlanan:** Adım 20.5 — Multi-language EN/TR (`IIM-85`, 2026-09-23) — 6 parça, tipli sözlük, kütüphane yok, 285 test hâlâ yeşil
- **Son tamamlanan:** Adım 20.7 — Metin denetimi (`IIM-92`, 2026-09-23) — 10 silme, 34 yeniden yazım, Türkçede bir sayı yanlışı ve her dialog'da bir ekran okuyucu etiketi
- **Son tamamlanan:** Adım 15 — YARP API Gateway (`IIM-93`, 2026-09-23) — tek origin, rota tablosu `vite.config.ts`'ten C#'a taşındı, 285 test yeşil
- **Son tamamlanan:** Adım 16 — JWT auth, roller ve organizasyon bazlı sahiplik (`IIM-94`, 2026-09-24) — 13 parça, beşinci servis, 10 entity + Elasticsearch + 3 hub org'a bağlandı, **341 test**
- **Son tamamlanan:** Adım 17 — OpenTelemetry distributed tracing (`IIM-109`, 2026-09-25) — 5 parça; OTLP push'undan bildirime **tek trace** Seq'te görüldü. Yolda bir Adım 16 regresyonu kapandı: outbox dispatcher'ı org kapsamı set etmiyordu, Adım 16'dan beri hiçbir analiz ES'e indekslenmemişti
- **Son tamamlanan:** Adım 13.5 — OTLP log ingest (`IIM-115`, 2026-09-25) — 5 parça; 300 olaylık fırtına 10 satır, toplamı 300; gerçek .NET OTel SDK'sıyla protobuf; **410 test**
- **Son tamamlanan:** Adım 16.5 — davetle kullanıcı yönetimi (`IIM-122`, 2026-09-25) — 7 parça; organizasyon ayarları yalnız Admin'e (GET dahil), iki-org izolasyon probu (**68/68**), davet/sıfırlama/parola değiştirme uçtan uca, konsolda Üyeler + herkese açık bağlantı ekranları; **462 test**
- **Son tamamlanan:** Adım 24 — çözülen olayı telemetriye geri bildirmek (`IIM-130`, 2026-09-25) — 6 parça; kapatırken karar, IncidentService'e outbox, sınırlı "geçmiş" puanı, telemetri tüketicisi; kanaryada yanlış alarm ve gerçek yolları uçtan uca, tek trace; **485 test**
- **Devam eden:** Adım 17.5 — AI analizi organizasyonun GitHub'ına MCP üzerinden bakabilsin (`IIM-137`); kapsam 2026-09-25'te konuşuldu. Ardından kalan adımlar **yeniden konuşulacak**

### Öncelik sırası (2026-09-21'de kararlaştırıldı)

Yol haritası **adım numarası sırasına göre değil**, aşağıdaki sıraya göre yürüyecek:

1. ~~**Adım 18 — testler.**~~ **Yapıldı** (`IIM-28`). Regresyon koruması artık var.
   Integration testleri (Testcontainers) bilinçli olarak kapsam dışı bırakıldı → ayrı adım.
2. ~~**Adım 19 — React frontend.**~~ **Yapıldı** (`IIM-35`). Sistem artık görünür.
3. ~~**Adım 19.5 — tasarım sistemi ve UX revizyonu.**~~ **Yapıldı** (`IIM-46`). Adım 19'un görsel
   katmanı seçilmemişti; login/logout yüzeyi Adım 16'nın önüne alındı ki auth geldiğinde değişecek
   tek şey `session.ts` olsun. Backend'e dokunulmadı.
4. ~~**Adım 20 — dashboard ve analitik.**~~ **Yapıldı** (`IIM-54`). Anasayfa artık veri taşıyor;
   soket kapsamı tamamlandı ve sessiz bir analiz hatası kapatıldı.
5. ~~**Adım 20.5 — i18n (EN/TR).**~~ **Yapıldı** (`IIM-85`). Adım 20'den sonraya bilinçli bırakıldı ki
   yeni ekranlar önce İngilizce yazılıp sonra ayrıca çevrilmesin. Fırat 2026-09-23'te **Adım 20.6'yı**
   da ekledi: AI yanıtlarının dili — arayüz dilinden ayrı bir iş, yol haritasında.
6. ~~**Adım 15 + 16 birlikte.**~~ **Yapıldı** — Adım 15 (`IIM-93`, 2026-09-23), Adım 16 (`IIM-94`, 2026-09-24).
   **Bu maddenin gerekçesi planlama sırasında ölçüldü ve yarısı yanlış çıktı**, düzeltmesiyle
   birlikte duruyor: *"JWT doğrulamanın doğal yeri gateway"* **değil** — org bazlı
   `HasQueryFilter` claim'i servisin içinde istiyor, yani `AddJwtBearer` dört servise zaten
   yazılacak ve gateway onu kurtarmıyor. Gateway'in gerçekten satın aldığı dört şey: **tek
   origin** (CORS'u yapılandırmak değil, gereksiz kılmak — dört servisin hiçbirinde `AddCors`
   yoktu), **token'ın localStorage'a girmemesi** (tek origin → httpOnly + SameSite=Strict
   cookie; dört origin olsaydı ya cross-origin cookie ya XSS'in okuyabildiği bir JWT), **üç
   soketin cookie ile doğrulanabilmesi** (aksi halde üç ayrı `accessTokenFactory` ve token
   URL'de), ve **tek yönlendirme tablosu** (dev ile prod'un aynı dosyayı kullanması). Ciddi
   karşı argüman yazılı kaldı: Adım 22 Kubernetes ise Ingress zaten gateway'dir — ama Ingress
   dev'de yok ve JWT doğrulamıyor. **Hub grupları Adım 16'nın içinde** — Adım 20 yayın
   yüzeyini genişletti ve org kapsamlaması soket üzerinden uygulanamıyor
   (`production_necessaries.MD` §2).
7. ~~**Adım 13.5 + 17 birlikte**~~ **Yapıldı** — Adım 17 (`IIM-109`), Adım 13.5 (`IIM-115`), 2026-09-25.
   Gerekçedeki "aynı bağımlılık" yarı doğruydu: ikisi aynı *tel formatını* paylaşıyor, aynı paketi
   değil. Tek kabul koşusunun ikisini birden kanıtlaması ise tam olarak işe yaradı.

**Yeni sıra (Fırat, 2026-09-25)** — "gördüğüm en önemli üç parça bunlar":

8. ~~**Adım 16.5 — davetle kullanıcı yönetimi.**~~ **Yapıldı** (`IIM-122`, 2026-09-25).
9. ~~**Adım 24 — çözülen incident'ı telemetriye geri bildirmek.**~~ **Yapıldı** (`IIM-130`, 2026-09-25).
10. **Adım 17.5 — MCP entegrasyonu.**
11. Kalanlar (21, 22, 23, 20.6, 13.6, Adım 20 kalıntısı, 14) bu üçü bitince **yeniden konuşulacak**;
    eski 8. maddenin 21 → 22 sırası da o konuşmaya dahil.

**24 ve 17.5 için kural:** koda ve plana başlamadan önce ne yapılmak istendiği Fırat'la açılıp
konuşulacak — kendi ifadesiyle "tam anlayamadım ne yapılmak istendiğini". Yol haritasındaki
maddeler kapsamı değil gerekçeyi taşıyor; o konuşmanın çıktısı plan olur.

**Önceliği düşürüldü:** Adım 14 (Comment & Timeline) — evidence API'si ve delivery geçmişi
audit hikâyesinin çoğunu zaten veriyor.

---

## Tamamlanan adımlar

- [x] **Adım 1** — Proje yapısı: BuildingBlocks (Contracts/EventBus/Observability/SharedKernel) + Services (AgentOrchestrator/IncidentService/NotificationService/TelemetryIngestionService)
- [x] **Adım 2** — BuildingBlocks içerikleri (SharedKernel: Entity/AggregateRoot+DomainEvents/DomainEvent/ValueObject; EventBus; Contracts: IncidentDetectedEvent; Observability)
- [x] **Adım 3** — RabbitMQ EventBus (EventBusOptions, RabbitMqConnection+Polly, RabbitMqEventBus)
- [x] **Adım 4** — Docker Compose (RabbitMQ, 4x PostgreSQL 5433-5436, Seq, pgAdmin; base/override/.env)
- [x] **Adım 5** — IncidentService Clean Architecture: Incident aggregate, CQRS, EF Core+Npgsql, migration + HTTP 201 + RabbitMQ publish doğrulandı
- [x] **Adım 6** — IncidentService CRUD tamamlama: List/pagination, UpdateStatus & AssignTeam, FluentValidation v12, ValidationBehavior, GlobalExceptionHandler (RFC 9457)
- [x] **Adım 7** — AgentOrchestrator + AI (çıplak Anthropic.SDK): IncidentAnalysis aggregate, jsonb mapping, event subscribe, geri-yazma döngüsü (IncidentAnalyzedEvent, ApplyAiAnalysis). Tam otonom uçtan uca döngü test edildi.
  - **Adım 8 / 9 / 11 bu adımın içinde tamamlandı** (IncidentDetectedEvent subscribe · AI priority/category · AI result write-back loop)
- [x] **Adım 7.5** — MAF refactor: Microsoft.Agents.AI 1.12.0 + Anthropic 1.12.0-preview, MafAiAnalyzer (AsAIAgent+RunAsync), DI tek satır geçiş, UsageDetails token counts. AnthropicAiAnalyzer yedek kaldı.
- [x] **Adım 10 — RCA (Parça 1 + A–H)** — Elasticsearch 9.4.3 + Kibana, Transactional Outbox (interceptor → atomik commit), OutboxDispatcher (at-least-once, dual-route RabbitMQ + ES), BM25 similarity search, MAF in-process tool-calling (search_similar_incidents) + kalibre edilmiş confidence. Uçtan uca doğrulandı.
- [x] **Adım 12** (`IIM-1`, 2026-09-16) — NotificationService: AI analizi bitince email/webhook/Jira bildirimi, müşteri konfigüre edilebilir `Integration` tablosuyla. Kanallar: email + webhook + Jira; lokal SMTP = Mailpit; config REST CRUD ile yönetilir. Uçtan uca doğrulandı.
  - [x] **Parça 1** (`IIM-2`, 2026-09-16) — EventBus: queue-per-service + DLQ. Kuyruk adı `SubscriptionClientName`, event başına routing-key binding; tek consumer channel (SemaphoreSlim); hata halinde `requeue:false` + servis başına DLQ (eskiden sonsuz requeue). Broker topolojisi + iki yönlü mesaj akışı runtime doğrulandı.
  - [x] **Parça 2** (`IIM-3`, 2026-09-16) — `IncidentAnalyzedEvent`'e `IncidentTitle` + `Confidence` eklendi (OutboxDispatcher map'liyor); integration event Id'si artık outbox satırının Id'si → retry'da stabil, idempotency anahtarı olarak kullanılabilir
  - [x] **Parça 3** (`IIM-4`, 2026-09-16) — NotificationService.{Domain,Application,Infrastructure,API} oluşturuldu (IncidentService düzeninin aynısı), `.slnx`'e eklendi, `NotificationDb` (5434) bağlandı, API http 5210 / https 7110. Artık `Services.NotificationService.csproj` çözümden çıkarıldı (dosyalar diskte; IncidentService/AgentOrchestrator'da da aynısı yapılmış — `**/*.cs` glob'u alt projeleri derliyordu)
  - [x] **Parça 4** (`IIM-5`, 2026-09-16) — `Integration` (channel + IsEnabled + jsonb config + MinPriority/CategoryFilter + `Matches()`) ve `NotificationDelivery` (unique `(IntegrationId, IncidentId)` = idempotency anahtarı) eklendi, `InitialCreate` migration uygulandı. `Skipped` statüsü düşürüldü (idempotency anahtarını işgal ediyordu); `IncidentPriority` yerel kopya (cross-domain referans yasak)
  - [x] **Parça 5** (`IIM-6`, 2026-09-16) — `INotificationChannel` (keyed DI, `NotificationChannelType` anahtarı) + `EmailNotificationChannel` (MailKit 4.18.0, ayarlar `Integration.Config`'ten), Mailpit compose'a eklendi (SMTP 1025 / UI 8025). Mailpit'e gerçek mail düştü. **Kapsam notu:** `POST /api/integrations/{id}/test` ve MediatR wiring Parça 8'den öne çekildi — her kanalı tek tek doğrulayabilmek için
  - [x] **Parça 6** (`IIM-7`, 2026-09-16) — `WebhookNotificationChannel` (named HttpClient + Polly retry; sadece 5xx/408/429/transport retry'lanır, 4xx final). Ayarlar: `Url`, `TimeoutSeconds`, `Header:<ad>` önekli serbest header'lar. Dev-only `POST /api/dev/webhook-echo` doğrulama hedefi. Başarı ve hata yolu ayrı ayrı doğrulandı
  - [x] **Parça 7** (`IIM-8`, 2026-09-16) — `JiraNotificationChannel` (v3 `POST /rest/api/3/issue`, Basic auth, ADF description). Gerçek Jira site'ına karşı doğrulandı: `IIM-12` (test ucu) ve `IIM-13` (uçtan uca koşu) oluştu, ADF paragrafları doğru render oldu
  - [x] **Parça 8a** (`IIM-11`, 2026-09-16) — `ValidationBehavior` → yeni `BuildingBlocks.Application`; `GlobalExceptionHandler` → yeni `BuildingBlocks.Web`; SharedKernel'e `NotFoundException` tabanı (handler eskiden `IncidentNotFoundException`'a bağlıydı, paylaşılamıyordu). NotificationService gerçek ikinci tüketici = YAGNI tetikleyicisi. IncidentService davranışı birebir aynı (400+errors / 404 doğrulandı)
  - [x] **Parça 8** (`IIM-9`, 2026-09-16) — `IncidentAnalyzedEvent` aboneliği + `DispatchNotificationsCommand` fan-out (filtre → idempotency → keyed kanal → `NotificationDelivery`), kanal başına hata izolasyonu, Integration CRUD (+ kanal başına zorunlu ayar validasyonu) ve `GET /api/notifications/incident/{id}` audit ucu. Credential görünümlü config değerleri okumada maskeleniyor. Tek event iki servise birden ulaştı, filtre ve idempotency doğrulandı
  - [x] **Parça 9** (`IIM-10`, 2026-09-16) — Uçtan uca doğrulandı: incident → gerçek AI analizi (Medium→Critical, kategori Application, confidence %82) → Outbox → RabbitMQ → hem IncidentService write-back hem NotificationService fan-out; 3 kanalın üçü de `Sent` (Mailpit maili, webhook echo, Jira `IIM-13`). 3 servis kuyruğu + 3 DLQ, DLQ'lar boş
- [x] **Adım 13** (`IIM-14`, 2026-09-21) — TelemetryIngestionService: dış log kaynağından çekme → imza → sinyal → **deterministik skorla** otomatik incident. Uçtan uca doğrulandı; AI analizi kanıt sayesinde 0.82 confidence'a çıktı ve gerekçesinde tekrar sayısını, süreyi ve oran anomalisini doğrudan alıntıladı. Telemetri bir *entegrasyon*: kaynak müşteri tarafından konfigüre edilir (Adım 12'deki `Integration` deseni), ilk connector Seq. Skorlamada AI yok; AI'a giden tek şey incident açıklamasına gömülen kompakt kanıt özeti.
  - **Signal ≠ Incident:** `≥0.90` incident · `0.60–0.89` zayıf sinyal (incident yok) · `<0.60` sadece kayıt (baseline + emsal beslenir)
  - **Dedup eskimesi:** açık incident var **ve** `now − LastSeenAt ≤ DedupWindow` (24s) → sayaç artar; TTL dolmuş ya da incident kapanmışsa **yeni** incident, öncekine bağlı
  - [x] **Parça 1** (`IIM-15`, 2026-09-17) — İskelet + 6 tablo + `InitialCreate` uygulandı. `LogRecord`'da `Timestamp` (kaynak saati) ≠ `IngestedAt`, clock-skew flag'i; `SourceCursor` ayrı tabloda ve geri sarmıyor; `ErrorSignature.CanAbsorbInto()` eskime kuralını tek yerde tutuyor; zaman sütunlarında BRIN index
  - [x] **Parça 2** (`IIM-16`, 2026-09-17) — `TelemetrySource` CRUD (jsonb config + kind başına zorunlu ayar validasyonu + credential maskeleme + `POST {id}/test`), `ITelemetrySourceConnector` keyed DI, Seq connector: `clef=true` ile NDJSON CLEF parse, `afterId` cursor'ı, boş batch'te pozisyon korunur. `ApiKey` opsiyonel (kimliksiz Seq'e de bağlanır); eksikse test ucu net hata veriyor
  - [x] **Parça 3a** (`IIM-24`, 2026-09-21) — `demo/MonitoredShop` (checkout servisi taklidi: sürekli trafik + `error-storm` / `timeout-storm` / `fatal` tetikleyicileri) + `seq-demo` (8082, `SEQ_FIRSTRUN_NOAUTHENTICATION` ile anonim okuma). Tespit verisi buradan gelir. **Kapsam düzeltmesi:** platform kendini değil, müşterinin sistemini izler. Canlı Seq'e karşı doğrulama Parça 2'de **iki gerçek bug** ortaya çıkardı: CLEF `@i` benzersiz id değil *event tipi hash'i* (bir fırtına tek satıra çökerdi), ve `afterId` ileri değil **geriye** sayfalıyor (tail cursor `fromDateUtc` olmalı)
  - [x] **Parça 3b** (`IIM-17`, 2026-09-21) — `BuildingBlocks.Observability.UsePlatformLogging()` ile 4 servis Serilog → `seq` (8081). `Service` damgası `TelemetryConstants.ServiceNames`'ten. Adım 12'de bulunan "Seq'e kimse yazmıyor" boşluğu kapandı; *tespit kaynağı değil*, sadece bizim gözlemlenebilirliğimiz. Ayrım korunuyor: servisler `seq`'e yazar, dedektör `seq-demo`'yu okur
  - [x] **Parça 4** (`IIM-18`, 2026-09-21) — Poller (hosted service sadece *ne zaman*a karar verir, iş `PollTelemetrySourceCommand`'da) + normalizasyon + fingerprint + `ErrorSignature` upsert. **Fingerprint mesaj şablonunu kullanıyor** — şablon yapısı gereği zaten normalize (değişkenler adlandırılmış yer tutucu), regex sadece şablonsuz kaynaklar için fallback. Doğrulandı: 110 kayıt → **2 imza** (80 + 30), 110 farklı event id (kopya yok), `Timestamp` ≠ `IngestedAt`
  - [x] **Parça 5** (`IIM-19`, 2026-09-21) — `DetectionRule` değerlendirmesi → `Signal` (kind=`LogBurst`) + imza başına z-score baseline (12 tam pencere; açık pencere hariç, düz baseline sınırlı skor, 5 örnekten az ise skor yok). Pencere başına tek sinyal (poll başına değil). Varsayılan kural boş tabloya bir kez seed'leniyor. Doğrulandı: 2 hata (eşik 3) → sinyal yok; 8 → tek sinyal; ikinci imza bağımsız sinyal
  - [x] **Parça 6** (`IIM-20`, 2026-09-21) — Outbox → `BuildingBlocks.Outbox`. İki dikiş yeri paylaşılabilir kıldı: `IOutboxStore` (satırlar hangi DB'de) ve `IOutboxMessageHandler` (mesaj ne anlama geliyor — eskiden dispatcher içindeki `switch`). Dispatch sırası korundu (önce yan etki, sonra `ProcessedOn`). `OutboxCleanupService` eklendi. Migration gerekmedi (EF: model değişmemiş); Adım 12 zinciri aynen çalışıyor
  - [x] **Parça 7** (`IIM-21`, 2026-09-21) — Deterministik skorlama (bileşen dökümü signal'de saklanıyor, AI yok) + TTL'li dedup + terfi + kanıt özeti → outbox → `SignalPromotedEvent`. IncidentId **telemetri tarafında** üretiliyor: at-least-once teslimde ikinci kopya, ikinci incident değil duplicate key olur. Test sırasında bug yakalandı: FATAL kısayolu eşik kontrolünden sonra geliyordu, tek crash eleniyordu — düzeltildi. Doğrulandı: FATAL tek seferde terfi, ikincisi açık incident'a yazıldı, üçüncüsü dedup dalından geçti, farklı imza bağımsız terfi etti
  - [x] **Parça 8** (`IIM-22`, 2026-09-21) — `SignalPromotedEvent` tüketimi → `Source=Telemetry` incident + `Incident.DetectedAt` (manuel oluşturmada da opsiyonel olarak var). Redelivery no-op (id promoter tarafından seçiliyor). `IncidentDto`'daki üç elle map tek `FromDomain`'e indi ve AI alanları artık dışarı veriliyor. Doğrulandı: `detectedAt 07:09:46` vs `createdAt 07:11:51`
  - [x] **Parça 9** (`IIM-23`, 2026-09-21) — `GET /api/telemetry/evidence` (insan + Adım 19 frontend'i için; AI tool'u **değil**) ve `GET /api/telemetry/signals` (varsayılan: zayıf band). Beş servisle tam uçtan uca koşu doğrulandı
- [x] **Adım 18** (`IIM-28`, 2026-09-21) — Unit testler (xUnit + NSubstitute + düz `Assert`). Yığın kararı 2026-09-21: FluentAssertions v8 ticari lisans istiyor, Moq'un SponsorLink geçmişi var — ikisi de alınmadı. **Integration testleri (Testcontainers) bu adımın dışında bırakıldı**; sonucu olarak `NotificationDelivery` yarış durumu ve EF mapping/jsonb/migration doğrulaması test edilmemiş kalıyor
  - [x] **Parça 1** (`IIM-29`, 2026-09-21) — Test iskeleti: `tests/` altında 4 proje (3 servis + BuildingBlocks), `.slnx` kaydı, xUnit 2.9.3 + NSubstitute 5.3.0. `dotnet test` yeşil (8 test). Her projede boş iskelet yerine gerçek bir assertion var ve bunlar bilinçli olarak sonraki parçaların tekrar etmeyeceği şeyler: tespit sabitleri + severity sırası, keyed DI'ın çözdüğü kanal enum'u, iki servisteki priority kopyalarının isim uyumu, outbox satırının pending varsayılanları
  - [x] **Parça 2** (`IIM-30`, 2026-09-21) — `SignalScoring` + `RateBaseline` (64 test). **Zayıf band (0.60–0.89) ilk kez doğrudan kapsandı**: eşiğin iki katı, korroborasyonsuz burst tam 0.65 → zayıf; aynı burst rate anomalisiyle tam 0.90 → terfi. Bandın tabanı sabit 0.60, tavanı kuralın — yani `PromoteThreshold` düşürülünce band kayar değil *daralır*. Ayrıca: fatal kısayolu diğer tüm terimleri atlıyor, doubling cap'i, sıfır eşik bölme koruması, z-score 2.0 dahil, clamp iki uçta, breakdown bileşenleri toplamı = kaydedilen total. `RateBaseline`: <5 örnek `null` (0 değil), düz baseline sınırlı 4.0, gürültülü baseline içindeki sayım anomali değil, açık pencere dışlanıyor, **boş zaman damgası listesi sıfırlanmış bucket döndürüyor** (boş liste dönseydi minimum örnek sayısının altına düşüp anomali bonusunu sessizce bastırırdı)
  - [x] **Parça 3** (`IIM-31`, 2026-09-21) — Telemetri domain (131 test). **`LogFingerprint` regex fallback'i ilk kez kapsandı**: maskeleme sırası kritik (GUID > uzun hex > sayı — sayı önce koşsaydı GUID `#-#-#-#-#` olurdu ve her müşterinin hatası ayrı imza görünürdü). **`ErrorSignature.CanAbsorbInto`** beş dal + pencerenin *son olaydan* ölçüldüğü (terfiden değil). **`LogRecord` clock skew** ilk kez tetiklendi. Ayrıca `DetectionRule`, `EvidenceSummary`. Not: fingerprint alan ayırıcısı `U+001F` — düz metinde görünmüyor, bir reformat sessizce silebilir; iki test tam olarak bunu yakalamak için var
  - [x] **Parça 4** (`IIM-32`, 2026-09-21) — `DetectSignalsCommandHandler` karar ağacının 19 dalı (150 test). **Bulgu: zayıf band, geçmişi olmayan imza için erişilemez.** Baseline 12 tam pencereden kuruluyor; geçmişi olmayan imza 12 *sıfır* bucket'a düşüyor → düz baseline → sınırlı 4.0 → anomali eşiğini geçiyor. Yani ilk burst'te anomali bonusu her zaman veriliyor: aynı 6 hatalık burst normal baseline'a karşı **0.65** (zayıf), boş baseline'a karşı **0.90** (terfi). Tasarım gereği doğru — bir haftadır sessiz imzanın ilk burst'ü gerçekten dikkate değer — ama eşik ayarlamadan önce bilinmesi gerekiyor. İki yarı da artık testle sabit
  - [x] **Parça 5** (`IIM-33`, 2026-09-21) — `Integration.Matches` filtre matrisi + `DispatchNotificationsCommandHandler` (27 test). Ağırlık hata yollarında: kanal patlarsa `Failed` yazılıp diğerleri gönderiliyor, kanal *çözülemezse* de aynı yere düşüyor, idempotency `(integration, incident)` bazında — kısmi hatadan sonra redelivery satırı olmayan entegrasyona ulaşıyor. `SaveChanges` hatasının yutulması bilinçli: bildirimler zaten gitti, rethrow DLQ'ya düşürüp redelivery'de hepsini tekrar gönderirdi — test tam olarak bunun "düzeltilmesini" engellemek için var
  - [x] **Parça 6** (`IIM-34`, 2026-09-21) — `Incident` aggregate (verilen id, `DetectedAt` ≠ `CreatedAt`, `ApplyAiAnalysis` idempotent ve statü/takımı bozmuyor), `CreateIncidentFromSignalCommandHandler` redelivery no-op + kaydetme *sonra* duyurma sırası, `OutboxDispatcher` (26 test). **Süitin gerçekten yakaladığı doğrulandı:** `OutboxDispatcher`'ın sırası bilerek ters çevrildi → üç test düştü → geri alındı
- [x] **Adım 19** (`IIM-35`, 2026-09-21) — React frontend (Vite + TS + Tailwind/shadcn + SignalR). Kararlar 2026-09-21: **tam yüzey, 6 ekran** · UI **Tailwind + shadcn/ui** · canlılık **SignalR (gerçek push, payload taşır)** · backend eklemesi `Confidence` + `SignalDto` zenginleştirmesi. **CORS alınmadı** → SPA yalnızca Vite proxy'sinin (`ws: true`) ardında çalışır; üretim cevabı Adım 15 gateway. State: sunucu state'i TanStack Query, filtreler URL'de, geçici UI state'i yerel — **global store yok**
  - [x] **Parça 1** (`IIM-36`, 2026-09-21) — Backend veri boşlukları (215 test). **`Confidence`** artık event → command → aggregate → `IncidentDto.AiConfidence` zincirinde akıyor; migration tek nullable kolon. `ApplyAiAnalysis` parametreyi **varsayılansız** alıyor — varsayılan, ileride bir çağrı yerinin alanı sessizce düşürmesine izin verirdi ki alan zaten tam olarak böyle kaybolmuştu. İmza değişikliği **dört çağrı yerini kırdı, dördü de testte, üretim kodunda sıfır** (Adım 18 ağının tam olarak yapmak için kurulduğu şey). **`SignalDto`** imzanın servis/exception/mesajını taşıyor (satır başına değil, batch başına tek okuma); imzası kaybolmuş sinyal etiketsiz ama listede kalıyor. **Pencere filtresi**: `from`/`to` verilince span **tüm statülerle** dönüyor — önce statüye filtrelemek, terfi edenle etmeyen arasındaki kontrastı gizlerdi ki span'a bakmanın tek sebebi odur
  - [x] **Parça 2** (`IIM-37`, 2026-09-21) — SignalR hub'ları: `/hubs/incidents` · `/hubs/notifications` · `/hubs/signals` (225 test). `IRealtimeNotifier` Application'da, `IHubContext` gerçeklemesi API'de — Application katmanı SignalR'ı bilmiyor. **Mesajlar HTTP ile aynı DTO'yu taşıyor**; tek istisna `incidentCreated` (yalnızca id — yeni satırın filtrelenmiş listenin 1. sayfasında durup durmadığı sunucunun sorusu). İki kural: **yayın kayıttan sonra** (yayınlanan, saklanan olmalı) ve **yayın hatası loglanır, fırlatılmaz** (yazma zaten commit'lendi; fırlatmak başarılı komutu 500'e çevirir ya da yan etkileri çoktan olmuş mesajı DLQ'ya düşürür). Dispatcher `SaveChanges` patlarsa kaydı yayınlamıyor. **Runtime doğrulandı:** üç hub da http profilinde WebSockets ile negotiate ediyor (`UseHttpsRedirection` engellemiyor, "Failed to determine the https port" uyarısı zararsız); gerçek bir PATCH singleton notifier'ı çözdü ve hatasız yayınladı
  - [x] **Parça 3** (`IIM-38`, 2026-09-21) — Frontend iskelet: `web/` (kökte, `.slnx`'te değil), Vite + React 19 + TS, Tailwind v4 + shadcn/ui (15 bileşen kaynak olarak kopyalandı), TanStack Query, React Router, `@microsoft/signalr`. **Vite proxy** yük taşıyan kısım: CORS yok, tarayıcı yalnızca dev server ile konuşuyor; hub rotalarında `ws: true` (olmadan WebSocket upgrade proxy'lenmez ve SignalR sessizce long polling'e düşer — çalışır ve hatayı gizler). Tipler elle yazıldı (codegen build'i çalışan servislere bağlardı). **Doğrulandı:** üç servis ayakken beş rota + hub negotiate proxy üzerinden 200. İlk probe iki servis kapalıyken 502 döndü — yanlış port da aynı görünürdü, o yüzden ikinci probe yapıldı
  - [x] **Parça 4** (`IIM-39`, 2026-09-21) — Incident listesi + detay. Filtreler ve sayfa URL'de (paylaşılabilir, yenilemeye dayanıklı); satırda tespit gecikmesi (`+2m 5s to open`). Detay bir form değil **zaman çizelgesi**. **Tarayıcıda bakarak iki kusur bulundu:** (1) select tetikleyicileri ham enum değerini gösteriyordu (`InProgress`, `any`) — etiket artık açıkça geçiliyor; (2) **"Analysis applied" yanlış zaman damgası gösteriyordu** — `updatedAt` her değişiklikte kayıyor, elle takım ataması yapılınca çizelge analizi o an olmuş gibi gösterdi. Incident kaydı analizin ne zaman uygulandığını saklamıyor; aşama artık zamansız, `updatedAt` ayrı "Last changed" satırında dürüstçe duruyor. **Açık kalan:** `AiAnalyzedAt` kolonu (`AiConfidence` ile birebir paralel) bu aşamayı düzgün gösterirdi — bilinçli olarak bu parçaya sokulmadı
  - [x] **Parça 5** (`IIM-40`, 2026-09-21) — Telemetri yüzeyi: **ısı haritası** + sinyal kuyruğu + kanıt görüntüleyici + "neden uyandırıldık" paneli. Harita servis × imza (ilk 8 + "other"), renk = tekrar sayısı, nokta = kapının nereye götürdüğü — iki kanal. **Renk ölçeği iki denemede oturdu:** plandaki log ölçeği gerçek veride *fazla* düzeltti (max 30 iken 30 ile 15 aynı tona düştü; log kısa aralığın üstünü yassıltıyor). Karekök ikisinin arasında: 30/15/1 ayrı basamaklara düşüyor, 80/2/2 de. **İkinci kusur ekranı okurken bulundu:** sinyalde *saklanan* gerekçe makinenin kültürüyle formatlanıyordu (`Confidence 1,00 ... 0,90`) — aynı sinyal hangi makinede tespit edildiğine göre kendini farklı açıklıyordu. `EvidenceSummary` zaten bilerek invariant; aynı kural ikinci yerde kırılmıştı. `tr-TR` altında koşan testle düzeltildi
  - [x] **Parça 6** (`IIM-41`, 2026-09-21) — Ayarlar ekranları (242 test). **Düzenleme formunu yazmak Adım 19'dan eski bir veri kaybı bug'ını ortaya çıkardı:** okuma credential'ı `***` ile maskeliyor, yazma config'i komple değiştiriyor ve maskeden haberi yok — yani `***` geri gönderilince müşterinin SMTP parolası gerçekten `***` oluyordu, anahtar atlanınca da siliniyor/validasyondan dönüyordu. Client gerçek değeri hiç görmediği için koruyamaz. `BuildingBlocks.Application/ConfigMasking` artık round-trip'in iki yarısını birden sahipleniyor (aynı "credential'a benziyor" tanımı hem gizlemeye hem geri koymaya karar veriyor); maskeye eşit değer "sakladığını koru" demek, validasyondan **önce** uygulanıyor. **İlk doğrulama denemesi kanıt değildi** — Mailpit her parolayı kabul ettiği için test geçmesi bir şey söylemiyor; Postgres'ten satırı okumak söylüyor: `***` gönderildikten sonra saklanan değer hâlâ `the-real-secret`. 16 yeni test
  - [x] **Parça 7** (`IIM-42`, 2026-09-21) — Realtime client: üç bağlantı, payload doğrudan cache'e yazılıyor; yalnızca `incidentCreated` invalidate ediyor. Reconnect'te ilgili key'ler bir kez invalidate (payload push'un gerçek maliyeti: soket düşükken gelenler kaybolur). **Kabul koşusu ölçülerek yapıldı** — `window.fetch` sarmalandı, "yeni HTTP isteği yok" iddia değil sayı: incident oluşturma 7→8 satır, **tam 1 istek** (id-only mesajın tasarlanan davranışı); sunucu tarafında takım değişikliği satıra `· platform` yazdı, **çağrı listesi boş**; timeout fırtınası ısı haritası hücresini 15→40 yaptı, **yine sıfır istek**. AI analizi de kendiliğinden düştü (Medium→Low, Other, %20 — sahte test incident'ı doğru şekilde önemsiz bulundu)
- [x] **Adım 19.5** (`IIM-46`, 2026-09-22) — Tasarım sistemi ve UX revizyonu. Adım 19 altı ekranı ayağa kaldırdı ama **görsel katman stok shadcn'di ve hiç seçilmedi**: `index.css`'teki her renk token'ı `oklch(x 0 0)` — chroma sıfır, tema baştan sona gri; `next-themes` bağımlılıkta duruyor ama hiçbir yere bağlı değil. Kararlar 2026-09-21: sahiplik modeli **organizasyon bazlı** · backend scoping **Adım 16'da** (bu adım backend'e dokunmuyor) · kapsam **tam revizyon** · tasarımı `ui-ux-designer` subagent'ı üretiyor
  - [x] **Parça 1** (`IIM-47`, 2026-09-21) — Tasarım sistemi + projenin **ilk subagent'ı** (`.claude/agents/ui-ux-designer.md`). Nötr rampa soğuk (hue 264) ve az da olsa chroma taşıyor — tam gri, geniş açık alanlarda "ayarlanmamış varsayılan" gibi okunuyor; aksan indigo-mor (274) ve **asla statü değil** (kırmızı/amber/yeşil zaten birer verdict tarafından tutulmuş, çakışan aksan operatöre ikisini birden görmezden gelmeyi öğretir). **Dört enum altı ortak tier'a indi** (alarm/elevated/caution/nominal/info/inert) — kırmızı her ekranda aynı şeyi söylüyor; **yalnızca `alarm` dolu zemin ve ona yalnızca üç durum ulaşıyor**: Critical, Failed, Promoted. `--heat-1..5` Parça 2'nin önünde geldi: sarı→kırmızı ve **temalar arası neredeyse değişmez** (tema değiştirince rengi değişen bir nicelik, aynı verinin farklı okunuşu olur); üstüne sayı basıldığı için ink iki temada da koyu — sarıda okunan ink kırmızıda okunmaz, o yüzden rampa tek ink'in yetebileceği banda sıkıştırıldı. `format.ts` artık **tek bir `dark:` ikizi yazmıyor**. Kabuk gruplu sol ray + dar ekranda drawer (Parça 4'ün alt navigasyonu buraya yeniden yerleşim olmadan giriyor). **120 kontrast çifti** oklch→sRGB→WCAG ile doğrulandı, sıfır hata, hepsi sRGB içinde
  - [x] **Parça 2** (`IIM-48`, 2026-09-22) — Isı haritası → **finans tarzı treemap**. `treemap.ts` elle yazılmış squarified layout (bağımlılık yok); kareleştirme olmadan naif dilimleme 400×3px slivers üretiyor — alanı dürüstçe taşıyor ama okunamıyor ve tıklanamıyor. Grup = servis, kutu = imza, **alan = tekrar sayısı**, **renk = tekrar sayısı** (sarı→kırmızı, pencere genelinde normalize). Karekök ölçeği aynen korundu; yalnızca hangi rampayı sürdüğü değişti. Hover'da kutunun **alt kenarından yukarı kayan panel** (sayaç + sinyal adedi + band + varsa incident linki); `prefers-reduced-motion` altında kayma yok, bilgi anında. **Band ikinci kanal olarak şekil oldu** (▲ terfi, ≡ dedup, ○ zayıf, ╱ bastırılmış, sadece-kayıt için işaret yok) — renk artık yalnızca niceliği taşıdığı için band'ın ayrı bir kanala ihtiyacı vardı. `maxColumns` 8 → `maxSignatures` 14 (alan, sütundan ucuz). Modül 300 rastgele koşuyla sınandı: kapsama tam 1.000000, sıfır örtüşme, sıralama hiç bozulmadı. **Kabul kriteri ölçüldü, iki kez** — agent 67→127, ben bağımsız olarak 80→120: kutu büyüdü ve kızardı, `window.fetch` + `XMLHttpRequest` sayacı **0**. **Bilinçli kabul:** 2/209'luk kutu 358×9px çıkıyor ve 24px hedef tabanının altında — küçük kutuları şişirmek alanın büyüklük anlamına gelmesini bozardı ki treemap'in yalan söyleyemeyeceği tek şey odur; WCAG 2.5.8 *essential* istisnası, her kutu klavyeyle erişilebilir ve altındaki liste aynı veriyi tam boyda veriyor
  - [x] **Parça 3** (`IIM-49`, 2026-09-22) — Integrations **kataloğu**: tablo + "Add" gitti, ikonlu kutucuklar geldi. **Desteklenen listesi elle tutulmuyor** — `integrationCatalogue.ts` `Record<NotificationChannelType, …>` tanımlıyor ve `notificationChannels` üzerinden map'liyor, yani union'a kanal eklemek bu dosyayı derlemede kırıyor ve backend'in gönderemediği bir kanal için kutucuk oluşamıyor; sınırı derleyici çiziyor. **Kutucuk = tip, örnekler içinde**: başlıkta sayaç, gövdede entegrasyon başına satır (switch / ad / filtre cümlesi / Test / Edit / Delete), altta `Add another`. Dialog'dan kanal seçici kalktı — kanal basılan kutucuktan geliyor, yani oluşturulduktan sonra değiştirilemeyen tek kontrol ortadan kalktı. **"Coming soon" kutucukları buton değil `div`** (dashed, sönük, odaklanamaz — tab taraması sıfır eleman döndürüyor); Opsgenie bilerek listelenmedi (Atlassian EOL duyurdu, batmakta olan ürüne "yakında" sözü vermek kataloğun yalan söyleyebileceği tek yer). **Marka ikonları monokrom** (`currentColor`, inline SVG, bağımlılık yok): bu konsolda renk bir verdict; dört doygun logo sayfanın en gürültülü şeyi olurdu ve hiçbir şey anlatmazdı, üstelik Slack ve PagerDuty'nin yeşili "sağlıklı" diye okunurdu. **Test geri bildirimi toast değil kalıcı satır şeridi** — 502 gövdesi (`HttpClient.Timeout of 2 seconds elapsing` gibi) ekranda kalıyor, toast onu çöpe atardı. Delete `confirm()` yerine ne kaybedildiğini (credential) ve ne kaldığını (teslimat geçmişi) söyleyen dialog. Sessizlik banner'ı iki gerçek durumu birden kapsıyor: hiç entegrasyon yok, ve hepsi duraklatılmış. **`buildConfig`'te ikinci bir veri kaybı bulundu ve düzeltildi** — şemada olmayan saklı anahtarlar her düzenlemede sessizce siliniyordu; canlı veride ikisi vardı (`Header:X-Test-Token` maskeli geri geliyor, yani **geri getirilemez** bir credential, ve `IssueType`). Denetlenmiş `buildConfig` mantığına dokunulmadan çözüldü: dialog saklı yabancı anahtarları `fields` dizisine ekliyor, değeri maskeyse `secret` işaretliyor, böylece aynı yoldan geçiyorlar. **Maskeleme kanıtı Postgres'ten**: iki kayıt sonrası `Name` ve `Port` değişti (PUT gerçekten indi), `Password` hâlâ `parca3-real-secret`
  - [x] **Parça 5** (`IIM-51`, 2026-09-22) — Login/logout + auth dikiş yeri. **Sıra değişti: Parça 4'ün önüne alındı**, çünkü profil ekranı `useAuth()`'a dayanıyor ve tersi sırada yer tutucu kimlik iki kez yazılırdı. **`session.ts` tek dikiş yeri** ve dosyanın başındaki sözleşme açık: ne kalıyor (dışa verilen yüzey + `Session` şekli), ne değişiyor (gövdeler token değişimine dönüyor), çağıranlar ne varsayabilir (oturum var) ve **ne varsayamaz** (kimsenin doğruladığı — yetkilendirme kararı sunucunun). **Parola alanı yok**: her şeyi kabul eden bir alan, operatöre bir credential'ın kontrol edildiğini öğretir, sonraki varsayım da verinin ona göre kapsandığı olur. Giriş kartı kullanıcıya da açıkça söylüyor ("No password, because nothing would check it"), sonra kalıcı ama sessiz biçimde hesap menüsünde "Unverified session" duruyor — her ekranın üstüne banner konmadı çünkü operatör oradan bir şey yapamaz. Organizasyon **seçilmiyor, gösteriliyor** (tek seçenekli select bir şey yapamayan kontroldür) ve rayda ürün adının altında sabit duruyor — her ekrandaki her sayının kapsamı o. `RealtimeProvider` router'ın üstünde kalıyor ama effect'i `isAuthenticated`'a bağlandı: giriş ekranında **sıfır negotiate**, girişte tam üç WebSocket, çıkışta üç temiz kopuş. Query cache `signIn`'de temizleniyor, `signOut`'ta değil — çıkışta uygulama hâlâ mount'lu, temizlemek her ekranı unmount olurken refetch'e sokardı. **Doğrulandı:** `?priority=Critical` ile çıkış yapılmış hâlde istenen URL → `/login` → giriş → **tam o URL**, filtre uygulanmış, "4 on record". `production_necessaries.MD` §2'ye 🔴 madde olarak yazıldı
  - [x] **Parça 4** (`IIM-50`, 2026-09-22) — Settings kabuğu + profil + telemetry'nin taşınması. `/settings` layout route'u, alt navigasyon **Profile · Telemetry · Integrations**; raydaki iki ayrı giriş tek **Settings**'e indi, iki seviye artık yarışmıyor. `/settings/telemetry-sources` → **`/settings/telemetry`**, eski yol için `replace` redirect (bir bookmark, bazı operatörlerin bir şeyin nerede olduğuna dair tuttuğu tek kayıt olabilir). **Profil ekranı dürüst**: "This is not an account — the name above is stored in this browser and nothing verified it… Real sign-in arrives with the gateway, and this page is where it will land." Appearance (Light/Dark/System) **gerçekten çalışıyor**. `TelemetrySourcesPage` katalog desenine geçti — tek `Kind` (Seq) olduğu için tek kutucuk + bağlı kaynaklar; "Coming soon" satırları yol haritasındaki **Adım 13.5 OTLP** ve **13.6 alert webhook**, ikisi de "loglarım Seq'te değil"in genel cevabı olduğu için dürüstçe öyle etiketli. `telemetrySourceKinds` `types/api.ts`'e eklendi — katalog union'dan türetiliyor, elle yazılmıyor. **Kullanıcıya görünen Türkçe string düzeltildi** (`"OTLP ingest is Adım 13.5."`). **Yabancı anahtar veri kaybı burada da kapatıldı**: Parça 3'teki düzeltme `ConfigFields.withStrayFields` olarak çıkarıldı (gerçek ikinci tüketici) ve iki dialog da onu kullanıyor. **Kanıt Postgres'ten, Test butonundan değil** — düzenleme gerçekten indi (`Name` değişti, poll 25→37) ve dört config anahtarı da sağlam: `ApiKey` (bildirilmiş sır), **`LegacySecret` (maskeli geri gelen yabancı anahtar — düzeltme öncesi sessizce silinecek olan)**, `Header:X-Trace` (düz yabancı anahtar), `Url`. Test butonu bağlanabildi-ama-eşleşme-yok ile bağlanamadı'yı ayırt ediyor; silme dialog'u neyin gittiğini (credential) ve neyin kaldığını (ingest edilmiş loglar ve imzalar — açılmış incident'ların kanıtı) söylüyor
  - [x] **Parça 6** (`IIM-52`, 2026-09-22) — Kalan ekranlar + **ölçülen kabul koşusu**. Incident listesine **gecikme kendi kolonu** oldu (`2m 5s / 2m 5s / 2m 5s / 2m 6s / —` diye aşağı okunuyor; karşılaştırma bu sayıyı göstermenin sebebi, elle açılanlar `0` değil `—`). Detayda **tespit gecikmesi şeridi** en üste çıktı: `detectedAt` → `createdAt` → boşluk büyük puntoyla, "noticed without being told"; elle açılan incident'a sahte sıfır değil **farklı bir şerit** düşüyor ("the platform was told rather than noticing") — sayıyı anlamlı kılan kontrast bu. Zaman çizelgesi aşamalarına **dört ayrı işaret şekli** geldi (oldu / kısmen / olmadı / hiç olmayacak iki ton gri değil) + aşama arası süreler. **`AiAnalyzedAt` tuzağı korundu**: "Analysis applied" `done · time not recorded` diyor, `updatedAt` ayrı "Last changed" aşamasında duruyor; çizelgenin açıklaması da bunu dürüstçe söylüyor ("Where a time is missing, it is missing from the record rather than from this screen"). Skor paneli **çizili sıfır ekseni** üzerine kuruldu — cezalar sola alarm kırmızısıyla, katkılar sağa; `fatal` yolu (`total` anahtarı yok) "scoring was bypassed" diye etiketliyor, toplam varmış gibi yapmıyor. `AiAnalysisPanel`'deki ham palet sınıfları kalktı ama **statü tier'larına eşlenmedi**: `nominal` "işe yaradı", `caution` "görüldü, yükseltilmedi" demek; modelin %93 emin olması ikisi de değil, bir ölçüm için verdict rengi ödünç almak yeşil=iyi sonuç öğretirdi. Tek `bg-primary` dolgu + band **kelime** olarak. `DeliveryStrip` kanal tipini ve dağıtım maliyetini (`createdAt`→`sentAt`: 26ms / 9ms / Jira 1.1s) gösteriyor. Kanıt ekranına **ingest gecikmesi** (`timestamp`→`ingestedAt`) kolonu eklendi — `LogRecord`'da hiçbir yerde render edilmeyen tek alandı. **Tooltip düzgün kuruldu**: bileşen zaten doğru Base UI'ydı, eksik olan mount edilmiş Provider'dı; `InfoHint` ile sarıldı ve **Base UI 1.8'de gerçek bir kusur bulundu** — popup'ta `role` yok, `id` yok, trigger'a `aria-describedby` verilmiyor, yani ekran okuyucuya süs; `InfoHint` kendi `aria-describedby`'sini `sr-only` kopyaya bağlıyor. Tooltip **satır başına değil ekran başına** üç yerde; 200 log satırına konmadı (200 tab durağı olurdu), oralarda sözlük **görünür metinle** yazıldı. **İki gerçek layout bug'ı 375px'te bulundu ve düzeltildi**: liste tablosu yatay taşıyordu, ve grid item'ın `min-width: auto` varsayılanı min-content olduğu için uzun bir stack-trace yolu **tüm dokümanı** genişletiyordu (`break-words` min-content katkısını azaltmıyor) — `min-w-0` ile çözüldü
  - **Kabul koşusu — ölçüldü, iddia edilmedi** (`window.fetch` + `XMLHttpRequest` sarmalandı): sunucu tarafında takım değişikliği `sre-oncall`→`database-guild` açık listeye düştü, **0 istek** · yeni incident 8→9 satır, **tam 1 istek** (`incidentCreated` id-only, tasarım gereği) · `timeout-storm` treemap'te TimeoutException 127→157, **0 istek**. `dotnet test` **242 yeşil** (23 + 31 + 22 + 166) — backend'e dokunulmadı, koşularak gösterildi. `npm run build` + `npm run lint` temiz (7 uyarı, hepsi önceden var olan ve dokunulmayan dosyalarda)

- [x] **Adım 20** (`IIM-54`, 2026-09-22) — Dashboard, analitik ve canlı geri bildirim. **Yol haritasındaki eski Adım 20'nin yerini alır.** Anasayfa veri bakımından yoksundu: `/` doğrudan `/incidents`'a yönleniyordu, tek liste vardı, sistemin ürettiği hiçbir toplam/eğilim/oran görünmüyordu. **Keşif:** dört serviste **tek bir toplama ucu yok** (tüm GET yüzeyi 8 uç, hiçbiri gruplamıyor) · ısı haritası zaten SignalR'da ama **yanıp sönme yok** · soket kapsamı yarım (entegrasyonlar, kaynaklar, kanıt hiç push almıyor; `['incident-signal']` **reconnect'te bile** düzelmiyor — kusur) · `incidentCreated` id taşıyor ama **DTO iki çağrı yerinde de bedava** · **AgentOrchestrator realtime-karanlık ve `MarkAsFailed` hiç domain event yaymıyor** → analiz patlayınca arayüz sonsuza kadar "bekleniyor" diyor (sessiz, kalıcı yanlış durum) · pagination boşluğu tek ekranda gerçek (`/signals` pencereli yolu **tamamen sınırsız**) · `CreatedAt` `protected set` olduğu için geriye dönük demo verisi API'den basılamıyor. **Kararlar:** ek ekranlar sinyal hunisi + servis sağlığı + bildirim sağlığı (**MTTR reddedildi** — `ResolvedAt` yok, `UpdatedAt` takım atamasıyla da kayıyor, uydurma olurdu) · i18n **ayrı adım** (20.5) · **grafik kütüphanesi yok, elle SVG** · demo verisi dev-only ve idempotent
  - [x] **Parça 1** (`IIM-55`, 2026-09-22) — Toplama uçları (**273 test**, +31). Üç yeni salt-okunur uç: `GET /api/incidents/stats` (günlük kova + priority/source/status kırılımı + açık incident dağılımı + tespit gecikmesi), `GET /api/telemetry/stats` (huni: log → imza → sinyal, bandlara göre; + servis başına rollup), `GET /api/notifications/stats` (entegrasyon başına Sent/Failed/Pending + gönderim süresi + son hata). **Günlük kovalama SQL'e itilmedi**: `CAST(timestamptz AS date)` oturumun saat dilimine göre çözülüyor, yani gün sınırı bağlantının nasıl yapılandırıldığına bağlı olurdu ve kimsenin kontrol edemeyeceği bir grafik çıkardı; bellekte kovalamak sınırı açık ve test edilebilir yapıyor. Bedeli beş skalarlık projeksiyonla ödeniyor — başlık, açıklama ve AI gerekçesi hiç tele çıkmıyor. **Açık incident sayıları pencereyi bilerek yok sayıyor** (altı hafta önce açılmış ve hâlâ açık olan, operatörün en çok görmesi gereken şey; tarih filtresi tam da onu gizlerdi). **Boş günler ve sıfır priority'ler dolduruluyor** — atlanan gün zamanı sıkıştırır ve bir fırtınayı normal hız gibi gösterir. **Medyan iki serviste de nearest-rank** (tek tanım; raporlanan süre gerçekten yaşanmış bir süre) ve **boş listede null, sıfır değil** — "hiçbir şey otomatik fark edilmedi" ile "anında fark edildi" zıt gerçekler. Saatleri uyuşmayan kayıt (negatif gecikme) yüzdeliklerin dışında tutuluyor ama yine de "fark edildi" sayılıyor. Huni'de **`Deduplicated` bilerek "uyandırılmadı" sayılmıyor** — zaten açık bir incident'a katlandı, yani biri daha önce uyandırılmıştı; saymak ürüne göstermediği bir itidali mal ederdi. **Sinyal ucundaki sınırsızlık kapatıldı**: pencereli yol (frontend'in her zaman kullandığı yol) `Take`'siz çalışıyordu; artık tavanlı ve **gerçek toplamı** taşıyan zarf dönüyor, sıralama da en yeniden eskiye çevrildi (bir pencereyi kesmek eski ucunu atmak demek). Kanıt ucunda imzalar ve sinyaller de aynı muameleyi gördü — üç koleksiyondan yalnızca biri sınırlıydı
  - [x] **Parça 2** (`IIM-56`, 2026-09-22) — Soket kapsamı tamamlandı (**285 test**, +12). Sekiz handler (entegrasyon + telemetri kaynağı × create/update/delete/set-enabled) artık notifier'ı çağırıyor; üç serviste de notifier zaten kayıtlıydı, sadece enjekte edilmiyordu. Silmede yalnızca id kaldığı için ayrı mesaj. **Yayın maskeli DTO taşıyor** — HTTP okumasının döndürdüğünün aynısı; her bağlı istemci aldığı için bunun testi de var. **İmza push'u eklendi**: `DetectSignalsCommandHandler` sinyali yayıyordu ama aynı transaction'da değiştirdiği `ErrorSignature`'ı (incident bağlanması, sayaçlar) yaymıyordu — kanıt ekranının imza sütunu, sinyaller aynı push'la canlı akarken bayatlıyordu. **Log kayıtları bilinçli olarak satır bazında yayılmıyor**: poll başına 200 satıra kadar yazılıyor, 5sn tabanında ~40 msg/sn ve grup olmadığı için herkese gider; yerine döngü başına tek `ingestionCompleted` özeti (ve hiçbir şey gelmediyse hiç mesaj yok — sağlıklı sistemin normal hâli boş poll'dür, kimsenin bir şey yapamayacağı heartbeat gürültüdür). **`incidentCreated` artık payload taşıyor** — DTO iki çağrı yerinde de bedavaydı; liste invalidate'i kalıyor (yerleşim sunucunun sorusu) ama detay cache'i ücretsiz doluyor, yeni gelen incident'a tıklamak sıfır istek. **Sessiz hata kapatıldı:** `IncidentAnalysis.MarkAsFailed` hiç domain event yaymıyordu → outbox yok → write-back yok → push yok → arayüz sonsuza kadar "analiz bekleniyor". Artık `IncidentAnalysisFailedDomainEvent` → outbox → `IncidentAnalysisFailedEvent` → IncidentService `Incident.AiAnalysisError`'ı yazıyor → **var olan `incidentChanged`** taşıyor; AgentOrchestrator'a hub açılmadı. `AiAnalysisError` **`IsAiAnalyzed=false`'tan farklı bir durum**: biri kendiliğinden çözülür, diğeri çözülmez; başarılı bir analiz hatayı temizliyor (redelivery ile retry var). Migration: tek nullable kolon. **Not:** üç hub da `Clients.All` — gruplar Adım 16'nın organizasyon kapsamlaması için önkoşul
  - [x] **Parça 3** (`IIM-57`, 2026-09-22) — Dev-only demo verisi. `POST /api/dev/seed-demo-incidents`, `IsDevelopment()` dışında 404. **API bunu yapamazdı**: `CreatedAt` ortak `Entity` tabanında `protected set`, yani her incident "şimdi" damgalanır ve 30 günlük grafik tek çubuğa çöker; setter'ı açmak üretim kodu için de açmak demekti, o yüzden değer **EF change tracker üzerinden** yazılıyor ve domain'e dokunulmuyor. Komut yolunu atlıyor — N satırlık basım açık her tarayıcıda N invalidation tetiklerdi. **İdempotent** (açıklamada `[demo-seed]` işareti; ikinci çağrı çoğaltmıyor — doğrulandı: 76 → ikinci çağrı 0 created / 76 alreadyPresent). Veri **bilinçli olarak çarpık**: düz rastgele serpiştirme değil, sakin taban + aralıklı kötü günler, çünkü grafiği çizmenin sebebi zaten incident varışlarının patlamalı olması. Sabit seed (aynı demo iki kez aynı şekli verir). Sekiz senaryo × dört priority × üç source × farklı kategoriler; telemetriden gelende `DetectedAt` var, elle açılanda yok (tespit gecikmesi ancak karşıtı varsa anlam taşır); ~%70 analizli, küçük bir dilim **analiz başarısız** (o durum artık var ve hiç göstermeyen bir demo ekranı kendi mutsuz yoluna karşı test etmez). **Canlı doğrulama:** 81 incident / 31 gün kovası / 23'ü dolu / en yoğun gün 8 · `bySource` 42 Telemetry, 20 Alert, 19 Manual · tespit gecikmesi medyan 229s, p95 370s · huni 260 log → 3 imza → 11 sinyal · bildirim sağlığı üç entegrasyonda 26 gönderim, medyan gönderim süresi webhook 0.07s'ye karşı e-posta 1.21s ve Jira 1.46s
  - [x] **Parça 4** (`IIM-58`, 2026-09-22) — Grafik ilkelleri + **Dashboard anasayfası**. `components/chart/` elle SVG, bağımlılık yok; `treemap.ts` ile aynı ayrım — karar veren kısım saf (`scale.ts`: `linearScale`, `bandScale`, `niceTicks`, `pickBandTicks`, `stackOffsets`), bileşen yalnızca çiziyor. Üç değişmez: hiçbir şey sıfıra bölmüyor, slotlar rayı tam kaplıyor (imleç her zaman tam bir bandın üstünde, ölü şerit yok), ve sıfır olmayan değer asla tabana düşmüyor. **`pickBandTicks` sondan geriye sayıyor** — zaman ekseninde referans en yenisi, o yüzden her zaman etiketli, ve geriye doğru tek adımlı stride çakışmayı umut edilecek bir şey olmaktan çıkarıp yapısal olarak imkânsız kılıyor. Bandlar arası boşluk adımla birlikte küçülüp kayboluyor (90 günde telefonda tüm adım 3px, 1px oluk verinin üçte biri olurdu). `useElementSize` `SignalHeatMap`'ten çıkarıldı — gerçek ikinci tüketici doğdu (YAGNI karşılandı, önceden değil). **Dashboard okuma sırası** 3am'de soruların sorulma sırası: ne bozuk → nasıl karşılaştırılır → az önce ne geldi → platformun kendi davranışı. **Pencere tuzağı**: her kart kendi kapsamını kelimeyle yazıyor, "Open right now" kalıcı `not windowed` rozeti taşıyor — hangi sayının seçiciyle hareket ettiği bir bakışta kontrol edilebilir olmalı, tooltip'te değil. **Null medyan** em-dash, asla sayı, ve **iki sebebi olduğu için iki cümlesi var** (hiçbir şey otomatik fark edilmedi / tüm farklar negatif döndü — saat kayması). UTC iki yerde birden yazılı, ve "son kolon bugün, hâlâ doluyor" da. **Grafik tek tab durağı, doksan değil**: ok tuşları imleci taşıyor, sabit `aria-live` günü ve priority kırılımını okuyor, `sr-only` tablo tüm sayıları taşıyor. Dört yeni priority token'ı (tier bir rozet; soluk tint + ink ikisi de çubuğa gerilince hayatta kalmıyor); ilk denemede Medium ile Low 1.14:1 ile okunamazdı, yeniden ayarlandı, komşu çiftlerin hepsi 1.6:1 üstünde, ve her segment üstüne kart renginde 1px saç teli çiziyor (ısı haritasının numarası) — sınır hangi iki dolgunun karşılaştığına bağlı kalmıyor. **Bulunan iki kusur:** gün etiketi telefonda SVG tarafından kırpılıyordu (sağ marj 24px); ve **demo seeder'ı gelecek tarihli satır yazıyordu** (bugünün kovası günün yalnızca bir kısmı, 0–24 saat seçmek bazılarını "şimdi"nin ötesine atıyordu; her pencere `now`'da bittiği için aynı veriyi okuyan iki ekran farklı toplam gösteriyordu) — seeder düzeltildi
  - [x] **Parça 5** (`IIM-59`, 2026-09-22) — **Sinyal hunisi · servis sağlığı · bildirim sağlığı.** Üçü de Parça 4'ün grafik ilkellerini kullanıyor, ikinci bir çizim katmanı yok. **Sıfır tuzağı yapısal olarak çözüldü: payda payla birlikte geliyor.** Manşet asla çıplak sayı değil — `0` büyük puntoyla, aynı satırda *"of 11 signals the gate scored"*. Okuyucu bunu boş pencere sanamaz, çünkü *bakılan*ın sayısı aynı cümlenin içinde. Üç ayrı durum, üç ayrı metin: sinyal yoksa manşet sayı hiç yok (filtreleme bir aşama önce olmuş, ya da hiç telemetri gelmemiş); `notRaised === 0` iken **"A zero here means the gate refused nothing, not that it looked at nothing"**; sıfırdan büyükken sayı + bir alerting kuralının yapamayacağı şey. `Deduplicated` **"acted on"** tarafında kendi paragrafıyla (biri uyandırıldı, sadece daha önce), ve beş band **SOMEBODY WAS WOKEN / NOBODY WAS WOKEN** diye gruplanıyor — bu ayrım ekranın tüm argümanı olduğu için dipnot değil, düzenin kendisi. 260→3 düşüşü tek ölçekte, her çubuğun arkasında çizili ray var (3 böylece boşlukta bir güdük değil, bir şeyin görünür %1'i) ve geçiş kelimeyle yazılı: *"About 87 records per signature — that fold is what fingerprinting bought"*; 3→11 aşaması **genişlediği için** huni gibi davranmak yerine bunu söylüyor. **Silinmiş entegrasyon satırını koruyor** (*"Deleted integration"*, id'si, ve teslimatların neden saklandığı) — `isEnabled` gitmiş bir satır için false'tur, ona "disabled" demek iki kelimeyle birden yanlış olurdu. **Null medyan her yerde `—`**, ve kart bunun bir ölçümün yokluğu olduğunu satır başına değil bir kez söylüyor; grafikte null çubuk çizmiyor ama rayını koruyor. Satır verdict'i `lastErrorAt`'i `lastSentAt` ile karşılaştırıyor: pazartesi patlayıp sonra teslim etmiş kanal **Recovered**, `Failing` değil — yalnızca `Failing` dolu alarm zemini alıyor. Rayda **üçüncü grup: Pipeline** (Operations yedi satır olacaktı; Operations "ne bozuk, ne yapayım", Pipeline "makine işini yapıyor mu" — farklı soru, farklı saat). Sıralama ve pencere URL'de; `Last signal` null'ları **iki yönde de sona** gidiyor (yokluk 1970 değil), `(signature gone)` sıralama ne olursa olsun gerçek servislerin altına sabitlenmiş. Canlı verinin göstermediği her durum tarayıcıda uydurulmuş payload'la denendi
  - [x] **Parça 6** (`IIM-60`, 2026-09-22) — Client realtime + **ısı haritası yanıp sönmesi** + liste sınırları. **Yanıp sönme sokete değil, haritanın kendisine bakarak türetiliyor** (`useHeatChanges.ts`): önceki harita ile diff — üç sebep, ve üçüncüsü kararı verdi: (1) geçici UI state'i bileşene ait, (2) push'u değil **her değişim sebebini** yakalıyor (reconnect refetch'i, pencereden düşen sinyal), (3) DOM düğümüne değil **hücreye** anahtarlı — treemap her push'ta yeniden yerleşiyor, kutucuk işaretlendiği karede boyut ve yer değiştirebiliyor. **Sönümleme kararı dört sayı**: `coalesceMs 600` (saniyede otuz sinyal bir kez yanıp söner, otuz kez değil), `burstLimit 4` (arası boşluksuz dört parti sonrası animasyon tamamen duruyor ve işaretler öylece duruyor), `quietMs 2500` (sessizlik animasyonu yeniden kuruyor, sayılarak değil iz sürerek), `markMs 4000` (işaret solmayı **bilerek** aşıyor: harita donduğunda tek sinyal bu, `prefers-reduced-motion` altında da tek sinyal bu). Vurgu `--heat-flash` (derin indigo) iç halka — rampayla yarışmıyor, çünkü renk zaten "ne kadar sıcak" demek. **`incident-signal` kusuru düzeltildi** (hiçbir `recoverKeys`'te yoktu, soket düşüp kalksa bile düzelmiyordu). Altı yeni handler bağlandı; `incidentCreated` payload'ı `['incident', id]`'yi dolduruyor, liste invalidate'i kalıyor. **`ingestionCompleted` sessizce refetch etmiyor** — kanıt ekranı "bu pencerede N yeni kayıt" diyor ve kararı operatöre bırakıyor; sessiz refetch soketin ortadan kaldırmak için var olduğu trafiği geri getirirdi. `/signals` **sayfalama değil büyüme**: iki şey yüklü dizinin tamamından kuruluyor (harita onu topluyor, kutucuk filtresi onu kesiyor), offset sayfalama haritayı 1. sayfadan listeyi 3. sayfadan çizerdi — ve push prepend ettiği için her offset sınırı bir kayardı. Buton kaç tane kaldığını yazıyor (`Load 36 more`). **Ölçüldü:** iki fırtına → iki yeni kutucuk (30 ve 25 tekrar), **2 eşzamanlı yanıp sönme** t+626ms'de (iki fırtınanın sinyalleri 600ms penceresinde birleşti — tasarlandığı gibi), 4004ms sonra söndü, **0 istek** · ikinci sekmede entegrasyon adı sunucudan değişti, ayarlar ekranı **0 istekle** güncellendi · `Load 36 more` → URL `?show=100`, **tam 1 istek**, buton kayboldu
  - [x] **Parça 7** (`IIM-61`, 2026-09-22) — Kabul koşusu ve dokümanlar. **Koşu bir boşluk buldu ve kapattı**: `AiAnalysisError` backend'de yazılıyor, DTO taşıyor, tip biliyor — ama **hiçbir ekran göstermiyordu**; incident detayı hâlâ "Waiting for the analysis service" diyordu, yani Parça 2'nin ortadan kaldırmak için var olduğu durumun ta kendisi. Alan iki parçanın arasına düşmüştü (Parça 2 backend'di, Parça 6 realtime'dı). Üç yerde birden kapatıldı: AI paneli (sağlayıcının kendi kelimeleriyle, kısaltmadan — "rate limit exceeded" ile "invalid API key" farklı aksiyon ister), incident listesi (`analysis failed`, rozet değil ink — satırda zaten müşterinin sistemi hakkında bir verdict taşıyan priority rozeti var, aynı ağırlığı vermek ikinci bir şiddet gibi okunurdu), ve zaman çizelgesi (**beşinci işaret şekli**: `pending` "henüz olmadı" demek, asla olmayacak bir aşama hâlâ olabilecek gibi durmamalı). **Ölçülen kabul:** `dotnet test` **285 yeşil** (23+38+41+183) · `npm run build` + `npm run lint` temiz · iki fırtına → iki yeni kutucuk, **2 eşzamanlı yanıp sönme** t+626ms, 4004ms sonra söndü, **0 istek** · sunucudan entegrasyon adı değişimi ikinci sekmede **0 istekle** göründü · `Load 36 more` → URL `?show=100`, **tam 1 istek**, buton kayboldu · **altı incident arka arkaya → 1 stats isteği** (6 değil; sayaçlar anında 56→62 ve 101→107 oldu, birleştirilmiş invalidate arkadan oturttu) · analiz hatası hem panelde hem çizelgede doğru render ediliyor. `production_necessaries.MD` §2'ye **hub gruplarının Adım 16'nın içinde yapılacak iş olduğu** yazıldı — Adım 20 yayın yüzeyini genişletti ve org kapsamlaması soket üzerinden uygulanamıyor

- [x] **Adım 20.5** (`IIM-85`, 2026-09-23) — **Multi-language (EN/TR).** Fırat'ın isteği. Adım 20'den sonraya bilinçli alındı: yeni ekranlar önce İngilizce yazılıp sonra ayrıca çevrilseydi iş iki kere yapılırdı. `CLAUDE.md`'nin "kod/yorum/commit/log İngilizce" kuralı bozulmuyor — değişen tek şey **kullanıcıya görünen metin**. **Ölçülen yüzey:** `web/src` **13.020 satır**, 38 dosyada metin, **~330 metin parçası / ~2.200 kelime**, 350–450 anahtar. Bu konsol alışılmadık derecede **düzyazı ağırlıklı** (tasarımın argümanı metnin kendisinde), yani Türkçe dosya mekanik bir çıkarma değil bir **yazım** işi. **Karar: kütüphane değil, tipli sözlük** — `react-i18next` yok; sözlük bir **nesne**, string anahtar yolu yok (`useT()` → alan erişimi), ve `tr.ts` `typeof en` olarak tipleniyor. Bedeli sıfır, karşılığı üç şey: eksik TR anahtarı **derleme hatası** (sessizce İngilizceye düşme değil), **yanlış anahtar yazılamaz** (450 anahtarlık mekanik diff'te sessizce yanlış cümle gösterme riski yapısal olarak yok), ve **imza uyuşmazlığı da hata** (EN'de `(count) => …` olan TR'de düz string olamaz). Çoğul `Intl.PluralRules`; Türkçe iki formu aynı yazar (*"3 sinyal"*, *"3 sinyaller"* değil) ve bu doğru Türkçe. Satır içi vurgu için `<T>` bileşeni `{name}` yer tutucularını ReactNode ile doldurur — **çeviri dosyasında HTML yok**. **Çevrilmeyenler yazılı:** `aiReasoning` / `signal.reason` / `lastError` / `ApiError` gövdeleri (sunucudan ve modelden gelen düzyazı — çevirmek ya backend i18n'i ya da model çıktısını uydurmayı gerektirir), enum **tel** değerleri (yalnızca gösterim çevrilir), config anahtarları ve servis adları. Sonuç **karışık dilli ekranlar** — Türkçe arayüz, İngilizce sunucu metni; kaçamak değil, yazılı karar
  - [x] **Parça 1** (`IIM-86`, 2026-09-23) — i18n çekirdeği. **Üç garanti ölçüldü, varsayılmadı**: `tr.ts`'ten anahtar silmek `TS2741`, fonksiyon girdisini düz string yapmak `TS2322`, `IncidentStatus`'a üye eklemek **hem `en.ts` hem `tr.ts`'i** kırıyor. **İki bağ çalışırken ortaya çıktı ve burada kapatıldı:** (1) `lib/format.ts` altı `Intl` formatlayıcısını modül yüklenirken **`undefined` locale** ile kuruyordu — tek dilliyken doğru, iki dilliyken yanlış; İngilizce tarayıcıda Türkçe seçen biri Türkçe kelimelerin üstünde İngilizce tarih okurdu. Artık locale başına kurulup locale ile anahtarlanmış cache'te tutuluyor. (2) **Provider'a `children` olarak geçen ağaç, provider'ın state'i değiştiğinde yeniden render olmuyor** — aynı element referansı geldiği için React bail-out ediyor. Yani tarih basan ama çevrilmiş kelime basmayan bir bileşen context tüketicisi değil ve eski locale'de kalırdı. `LocaleBoundary` sayfa ağacını dile göre key'liyor; query cache'in ve üç hub bağlantısının **altında** duruyor, o yüzden ne veri ne soket etkileniyor — bedeli kodda yazılı: dil değişiminde scroll ve açık dialog sıfırlanıyor. `index.html`'e ikinci bir pre-paint bloğu (`<html lang>`; yanlış kelime flaşı için değil — provider dili initial state'te çözüyor — **React'e hiç bakmayan okuyucular** için: ekran okuyucu, `:lang()`, tarayıcının çeviri teklifi). **Plandan bir sapma:** ayrı `lib/labels.ts` yok; dokuz enum etiket haritası sözlüğün içinde, `byKey<Union>()` yardımcısının ardında — union'a üye eklemenin build'i kırma özelliği **aynen korundu** (yukarıdaki tabloda kanıtlı), bir modül ve bir dolaylama eksiğiyle. `lib/window.ts` etiketlerini tamamen kaybetti: preset artık dakika + id, etiket sözlükte, ve **cümle içi biçim ayrıca yazılıyor** çünkü Türkçe bir string üzerinde `toLowerCase()` bir görüntü dönüşümü değil, kendi kuralları olan bir dil işlemi. **Tarayıcıda ölçüldü:** tarayıcı `tr` istiyor → konsol ilk karede `lang="tr"` ile Türkçe açıldı; İngilizceye geçiş tarihi `23 Eyl 2026 17:30:00` → `23 Sept 2026, 17:30:00`, sayıyı `124.038` → `124,038`, göreli zamanı `2 sa önce` → `2h ago` taşıdı ve `/signals?window=7d` URL'i remount'tan sağ çıktı. **Bir bulgu, not edildi ve kendiliğinden kapanıyor:** `lang="tr"` altında CSS `uppercase` Türkçe büyütme kuralını uyguluyor, o yüzden hâlâ İngilizce olan sekiz bölüm başlığı `PİPELİNE` diye çıkıyor — sekizinin de denetlendi, **hepsi kabuk, hiçbiri veri değil** (servis adı, fingerprint, enum tel değeri hiçbiri case dönüşümünden geçmiyor), her biri kendi parçasında düzeliyor
  - [x] **Parça 2** (`IIM-87`, 2026-09-23) — Kabuk ve oturum. **Etiket taşıyan üç dizi id taşımaya döndü** — rayın `navigation`'ı, ayarların `pages`'i, login yan panelinin `points`'i — üçü de `keyof Dictionary[...]` ile sözlüğe anahtarlı. Üçünden birine **iki dilde birden etiketi olmadan** yeni bir hedef eklenemiyor, ve bunu söyleyen bir yorum değil derleyici. `RealtimeStatus` `RealtimeProvider`'dan export edildi ki sözlük onun üzerinde **bütün** olmaya zorlansın: kelimesi olmayan bir durum boş bir çip olarak çizilirdi, ve o kontrolde boş çip sağlıklı olandan ayırt edilemez. **İki birleştirme:** `signOut` `common`'a taşındı (hesap menüsü ile profil kartı aynı eylemi ayrı ayrı yazıyordu — bir ürün "Sign out" ile "Log out"u böyle aynı anda söylemeye başlar); ürün adı `BrandMark`'a `productName` olarak çıktı — özel isim olduğu için çeviri değil, ama üç yerde yazılıydı ve Parça 6'nın taraması görünür bir metnin neden sözlükte olmadığına savunulabilir bir cevap istiyor. **Listedeki iki dosyada hiç metin çıkmadı**: `sonner.tsx` ve `RequireAuth.tsx` — sayaç birinde CSS custom property'lerini, diğerinde geliştiriciye bakan bir `throw`u yakalamış; ikisine de dokunulmadı. **Ölçüldü:** ray `Panel · Olaylar · Sinyaller · Kanıt · Huni · Servisler · Teslimatlar · Ayarlar`; Parça 1'de not düşülen `PİPELİNE` izi bu başlıklar için kapandı — artık `OPERASYON` ve `BORU HATTI`, yani İngilizce kelimeye uygulanan Türkçe büyütme değil, Türkçe kelimenin doğru büyütülmesi; login ekranı 800px'te iki kolonlu düzenini Türkçe taşmadan koruyor
  - [x] **Parça 3** (`IIM-88`, 2026-09-23) — Incident ekranları. **`format.ts` son metnini de bıraktı**: `scoreTermLabel` ve `scoreTermHelp` bir biçimlendirme modülünde yaşayan İngilizce düzyazı haritalarıydı; kelimeler sözlüğe taşındı, geride **arama ve geri düşüşü** kaldı — çünkü geri düşüş metin değil **davranış**: kapının sonradan kazandığı bir terim, bu sürüm o kelimeyi öğrenmiş olsun olmasın, camelCase bölmesi üzerinden yine satırını ve sayısını alıyor; aritmetiğin ekranda tutması gerekiyor. Öğretilmiş sekiz terim `byKey<ScoreTerm>`'den geçiyor, yani **iki dilde de bütün**, küme ise açık kalıyor. **Çeviri sırasında dört çıplak enum değeri bulundu** — listedeki priority rozeti ve source kolonu, detaydaki source rozeti, teslimat şeridindeki status ve channel. Bunlar **zaten İngilizcede de yanlıştı**: `InProgress` yalnızca birinin kaçınmayı hatırladığı yerlerde kaçınılmıştı, ve etiketleri tek yere toplamak bu dördünü görünür kıldı. **`plural()` burada hak ediyor** ("3 occurrences", "2 attempts", "1 of 3 channels delivered") ve her sözlük onu **kendi diliyle** çağırıyor — hem doğru hem de import döngüsü olmamasının sebebi: `en.ts` `plural('en', …)` diyor, iki formu birebir aynı olan Türkçe dosya ise hiç çağırmıyor. **Çevirinin ortaya çıkardığı bir şey:** zaman çizelgesi `+{stage.since}` basıyordu — artı bileşenin, kelimeler sözlüğün. Bu, kelimeler sayıyı izlediği sürece çalışır ve izlemediği anda çalışmaz; artı string'in içine taşındı (`+3s to detect` / `tespit için +3s`). **Sınır korundu:** AI panelinin hata dalı sağlayıcının kendi kelimelerini hâlâ kısaltmadan ve çevirmeden basıyor, `aiReasoning`, `signal.reason` ve `delivery.lastError` de öyle. **Ölçüldü:** `/incidents` → `Olaylar`, başlık `Öncelik · Olay · Kaynak · Durum · Analiz · Tespit · Gecikme`, iki filtre de `Her durum` / `Her öncelik`
  - [x] **Parça 4** (`IIM-89`, 2026-09-23) — Telemetri ekranları. Tarama script'iyle ölçüldü: uygulama genelindeki sayım **293 metin parçası / 1.937 kelime**den **136 / 889**a düştü, ve kalan 136'nın hepsi Parça 5'in dosyalarında. **Ekran başına büyümüş üç ayrı sözcük dağarcığı teke indi**: `kindLabel` (sinyaller), `bandTitle` (ısı haritası), `verdictMeaning` (huni) — her biri ürünün paylaştığı bir dağarcığın tek ekrana ait özel kopyasıydı; artık sözlükte ve union olan yerde `byKey<SignalStatus>`, yani yeni bir band bir dilde boş çizilmek yerine **iki dilde birden derlemeyi kırıyor**. **Huninin elle yazılmış `plural`'ı gitti** (`` ${word}${count === 1 ? '' : 's'} ``) — bir kelimeye `s` eklemek başka hiçbir dilin paylaşmadığı bir kural, ve o tek dosyada sekiz kez çağrılıyordu. `describeFolding` / `describeBursting` `useT` çağırmak yerine **sözlüğü argüman alıyor**: bunlar bileşen değil, render gövdesinden çağrılan düz fonksiyonlar, ve hook'a çevirmek üstlerindeki `funnel.signals === 0` dalının arkasına bir hook koyardı — boş pencereyi bekleyen bir koşullu-hook hatası. **`<T>`'nin ilk gerçek kullanımı**: huninin sıfır manşeti cümlenin ortasında vurgulu bir yan cümle taşıyor (*"A zero here means the gate refused nothing, not that it looked at nothing"*) ve o yan cümle, sayının boş pencere sanılmasına karşı ekranın tüm savunması. **Tek** sözlük string'i olarak, adlandırılmış bir yuvayla kaldı: çeviri dosyasına markup girmiyor ve vurguyu sonraki dili yazan sessizce düşüremiyor. **`(signature gone)`** gösterim için çevrildi, literal karşılaştırma anahtarı olarak kaldı — enum'a yapılanın aynısı; bu sunucunun sentinel'ı, düzyazı değil, ve Türkçe bir tablodaki tek İngilizce satır hata gibi okunur. **Yolda iki çıplak enum değeri daha bulundu**: kanıt ekranındaki log satırlarının severity kolonu ve hunideki severity kırılımı. **Türkçe dosyanın ortaya çıkardığı bir şey:** birkaç girdi hiç kullanmadığı bir parametre alıyor (`_count`, `_word`, `_signatureCount`) — Türkçe sayıdan sonra ismi çekimlemediği için ("3 sinyal", asla "3 sinyaller"), İngilizcenin kelime formunu seçmek için ihtiyaç duyduğu sayı Türkçe cümlenin parçası değil; parametre `en.ts`'e atanabilir kalmak zorunda olduğu için duruyor, alt çizgi ise eksikliğin bilinçli olduğunu söylüyor ve gerekçe `tr.ts`'in başında yazılı. **Ölçüldü:** `/signals` → `Sinyaller`, *"Tespit kapısının baktığı her şey — kimseyi uyandırmamaya karar verdikleri dahil."*, pencere `Son 2 saat`; `/funnel` → `Sinyal hunisi`, pencere `Son 24 saat`
  - [x] **Parça 5** (`IIM-90`, 2026-09-23) — Dashboard, ayarlar, teslimatlar. **Tarama 136 metin parçasından sıfıra indi** — `web/src` altında sabit yazılmış tek bir kullanıcıya görünen metin kalmadı. **İki elle yazılmış çoğullayıcı daha gitti**, ikisi de `DeliveriesPage`'de: biri `s` ekliyordu, ikincisi **yalnızca birincisi "delivery" konusunda yanıldığı için vardı**. Huninin ki ile birlikte, bileşenlerin içine yazılmış üç ayrı İngilizce dilbilgisi kopyası, hangi dilde yazdığını bilen tek bir yardımcıyla değişti. **İki katalog kelimelerini bıraktı**: `CatalogueEntry` ve `SourceCatalogueEntry` ad ve özet taşıyordu; ikisi de sözlüğün üzerinde bütün olduğu bir union ile anahtarlı olduğu için ad `labels.channel` / `labels.telemetryKind`'dan, özet ayarlar grubundan geliyor — o dosyalarda kalan **işaret**, ki o dil değil. **`configSchema.ts` bir `ConfigFieldId` union'ı kazandı** ve etiketlerini bıraktı: config anahtarı tek başına bir alanı adlandıramıyordu — `Url` bir connector'da webhook ucu, diğerinde Seq örneği, ve aynı etiketi paylaşmıyorlar; sözlük id üzerinde bütün, yani bir alan **iki dilde de etiketi olmadan tanımlanamıyor**. **Placeholder'lar kaldı** (`incidents@example.com`, `localhost`) — müşterinin üstüne yazdığı örnek değerler, düzyazı değil. **Stray yolu yapısal olarak hiç ellenmedi**: şemanın tanımlamadığı saklı bir anahtar hâlâ kendi config anahtarıyla çiziliyor, çünkü bu formun hiç öğretilmediği bir ayara verilecek başka ad yok — ve o yol zaten iki kere credential veri kaybı bug'ı üretti, o yüzden yalnızca taşıdığı hint taşındı. **Yeni bir dosya, import döngüsü sebebiyle**: `plannedIds.ts` — `en.ts` iki union'a, kataloglar değerlere ihtiyaç duyuyor, ve bunları kataloglardan birine koymak sözlüğün kendisini import eden bir modülü import etmesi demekti. **Bir çoğaltma kaldırıldı**: `dashboard/window.ts` `dayWindowLabel`'ı bıraktı, sözlükte zaten `window.dayScope` vardı ve aynı pencereyi adlandıran iki fonksiyon böyle ayrı düşer. **`<T>` üç kez daha kullanıldı**, üçü de aynı şekilde — ortasında tek bir vurgulu span taşıyan bir cümle: grafiğin altındaki UTC notu, servisler dipnotu, ve iki ayar ekranının paylaştığı test sonucu şeridi. **Ölçüldü:** `/settings/integrations` baştan sona Türkçe — `Entegrasyonlar` · `ŞU ANDA MEVCUT` · E-posta / Webhook / Jira özetleriyle · `YAKINDA` dört planlanmış hedefle · her kutuda `Bağlı değil.`
  - [x] **Parça 6** (`IIM-91`, 2026-09-23) — Kabul koşusu ve dokümanlar. **Tarama kalıcı bir script oldu**: `web/scripts/find-untranslated.mjs` (`npm run check:i18n`), bulduğunda 1 ile çıkıyor. Derleyicinin göremediği yarıyı o kapatıyor — `tr.ts`'in eksik olamaması tip sistemiyle garanti, ama **hiç anahtar yapılmamış** bir cümle mükemmel İngilizce çizilir ve asla çevrilmez. İstisnalar tek tek yazılı (placeholder'lar, tescilli adlar, `ApiError.name`, iki sentinel), çünkü sessiz bir allow-list böyle bir taramanın bir şey bulmayı bırakma şeklidir. **Koşu üç gerçek kusur buldu ve üçü de düzeltildi:** (1) 320px'de **yatay taşma** — `scrollWidth 412 / clientWidth 320`; `Brand`'in dış span'inde `min-w-0` yoktu, yani içindeki `truncate`'in daralacağı bir zemin yoktu ve tüm ad başlığı viewport'tan geniş yapıyordu. Türkçede ortaya çıktı çünkü realtime çipi `offline`'dan bir kelime uzun (`çevrimdışı`) ve bunu gizleyen boşluğu harcadı. (2) `/settings/telemetry` 484/320 — `PlannedRow` bir **grid item** ve bir grid track'inin tabanı, aksi söylenmedikçe içeriğidir; `min-w-0` eklendi. Yine Türkçede çıktı: `OTLP log alımı` ve özeti, entegrasyonlar grid'indeki her şeyden uzun. (3) **`Default organisation` çevrilmiyordu** — tarama buldu. Bu, bu konsolun uydurduğu bir sentinel, müşterinin seçtiği bir ad değil; `organizationName()` ile **gösterimi çevriliyor, saklanan değer anahtar kalıyor** — `(signature gone)`'a yapılanın aynısı, ve test metne değil **id'ye** bakıyor çünkü gerçek bir organizasyon adı Adım 16'da sunucudan gelecek ve asla çevrilmemeli. **Ölçülen kabul:** `npm run build` + `npm run lint` temiz · `npm run check:i18n` **sıfır** · **`dotnet test` 285 yeşil** (23 BuildingBlocks · 41 Notification · 183 Telemetry · 38 Incident) — backend'e hiç dokunulmadığının kanıtı · on bir rotanın hepsi **320px'de taşmasız**, iki dilde ve iki temada · `<html lang>` ilk boyanan karede doğru · erişilebilirlik ağacı Türkçe (`İçeriğe geç`, `Menüyü aç`, `Dili değiştir`, `Temayı değiştir`, `Hesap — Fırat Şakar`) · dil değişimi tarihi ve sayıyı da taşıyor (`23 Eyl 2026 17:30:00` ↔ `23 Sept 2026, 17:30:00`, `124.038` ↔ `124,038`). **Servisler ayağa kaldırılıp koşu veri üzerinde tekrarlandı ve iki şey daha buldu:** (1) **`renderSpan`'in birim ekleri çevrilmemişti** — aynı incident satırında `20 sa önce` (çevrilmiş) ile `1m 30s` (çevrilmemiş) yan yana duruyordu; tarama görmedi çünkü bunlar `format.ts` içinde template literal, JSX metni ya da etiketli bir prop değil. Birimler sözlüğe taşındı (`1dk 30sn`), **ayrı girdiler olarak** — bir dil sayıyı başka yere koymakta serbest. (2) **`InfoHint`'in `sr-only` kopyası `uppercase` miras alıyordu**: ipucu kullanan iki yer `uppercase` bölüm başlığı, yani okunmak için yazılmış bir cümle dönüştürülüyordu. Görsel olarak zaten görünmez, ama dönüşümün ekran okuyucuya verilenden güvenilir şekilde çıktığı kesin değil — `normal-case` eklendi. **İngilizcede de vardı**, i18n'in getirdiği bir şey değil. **Veri üzerinde doğrulandı:** incident listesi (108 kayıt, `Kritik`/`Yüksek`/`Orta`/`Düşük`, `Telemetri`/`Elle`, `Sayfa 1 / 6`), incident detayı (zaman çizelgesi, skor, AI paneli — **`aiReasoning` doğru şekilde İngilizce geçiyor**), huni (`0 / kapının puanladığı 2 sinyal` + sıfır savunması), ısı haritası ve efsanesi. Veri ekranında dil değişimi: `/signals?window=24h` korundu, `21 sa önce` → `21h ago`, `2 kutucuk · 55 kez` → `2 tiles · 55 occurrences`. **Bir prob yan etkisi not edildi ve elendi**: tarayıcı konsolundan `import('/src/lib/i18n/active.ts')` uygulamanınkinden **ayrı bir modül örneği** veriyor (Vite dev sunucusunda URL kimliği), o yüzden `activeLanguage()` 'en' diyordu; uygulamanın kendi grafiği üzerinden ölçüldüğünde (`format.ts`) hem `Intl` locale'i hem sözlük Türkçe, DOM de öyle — uygulama davranışı değil, probun kendisi

- [x] **Adım 20.7** (`IIM-92`, 2026-09-23) — **Metin denetimi ve açıklayıcı alt başlıkların kaldırılması.** Fırat'ın isteği: *"sayfalardaki sub infolar bence profesyonel durmuyor ... sayfanın veya alanın ne olduğunu açıklayan yazılar çok amatörce kaldıralım"* — örneği Ayarlar'daki `Profiliniz, bu platformun okuduğu log kaynakları, ve bulduklarını gönderdiği hedefler.` **Neden şimdi mümkün:** Adım 20.5 tüm kullanıcı metnini tek yere topladı (~450 girdi), yani ürünün sesi **ilk kez baştan sona tek oturumda okunabiliyor**; metin Adım 19/19.5/20/20.5 boyunca ekran ekran büyüdü ve tek bir ekranın içinden görünmeyen sapma olması beklenir. **Tutması gereken ayrım:** bu konsoldaki düzyazının bir kısmı **taşıyıcı** — huninin sıfır manşeti (`0` tek başına *hiçbir şeye bakılmadı* diye okunur, ki ekranın savunduğu iddianın tam tersi), dashboard'un `pencereden bağımsız` rozeti (hangi sayının seçiciyle hareket ettiğini söyleyen tek şey), her boş durum (ekranda birebir aynı görünen iki sebebi ayırıyor), null medyanın *sıfır değil, ölçümün yokluğu* cümlesi, login ve profil kartlarının *bu bir hesap değil* ifadesi. Okuyucunun zaten gördüğünü anlatan satır hedef; bir kararı taşıyan, bir kapsamı adlandıran ya da belirli bir yanlış okumayı engelleyen satır değil. **Yöntem:** işi yapan yeni bir kalıcı agent — `.claude/agents/content-strategist.md`, projenin ikinci agent'ı (`ui-ux-designer`'dan sonra). Tek bir yargı soruyor: *bu cümle silinseydi okuyucu neyi yanlış anlardı?* Cevap "hiçbir şey" ise gidiyor; belirli bir yanlış okuma ise kalıyor ve raporda hangi yanlış okuma olduğu tek bir yan cümleyle yazılıyor. **Agent'ın bulduğu ve uygulanan: 10 silme · 1 yeni anahtar · 34 yeniden yazım · 27 noktalama · 4 terim süpürmesi.** Onbir alt başlığın **yedisi silindi**, dördü taşıyan yan cümlesine indirildi: `signals` (bir ekranın adı "Sinyaller" ise alarm veren şeylerin listesi diye okunur, oysa satırların çoğu bilerek vermedikleri — bunu ekranın üstünde başka hiçbir şey söylemiyor) · `deliveries` (bu ekranı olay detayındaki Bildirimler panelinden ayıran yarısı) · `settings.integrations` ("Bir kanal birden çok entegrasyon tutabilir" cümlesi olmadan kutucuk ızgarası kanal başına tek entegrasyon gibi okunuyor) · `settings.telemetry` (*"Tespit, bu platformun kendisini izlemez"* — operatörler bir gözlemlenebilirlik ürününün kendi servislerini izlediğini varsayar, bu ürün izlemiyor ve bunu başka hiçbir yer düzeltmiyor). **Türkçede üç şey bulundu:** (1) **bir sayı yanlışı** — `deliveries.totals.failedOf` *"toplam 90 teslimat başarısız oldu"* basıyordu, yani yanındaki `0`'ın tam tersini iddia ediyordu; (2) **`Failing` ile `Failed` aynı satırda ikisi de "Başarısız"dı** — kanalın süregelen durumu ile bir teslimatın nihai durumu, ki ayrım tam orada önemliydi (`Ulaşmıyor` oldu); (3) **`ve` önünde 16 virgül** — TDK almaz, İngilizceden birebir taşınmış ve dosyayı "çeviri gibi" okutan asıl şey buydu. **Terim süpürmesi:** `destination` emekli edildi (kanal = tür, entegrasyon = yapılandırılmış örnek; bir ekranda iki şey için üç ad, adlardan birinin kusurlu olmasından kötü) · `the line` / `the threshold` aynı kartta iki addı, `threshold` seçildi — Türkçe zaten `eşik`te tutarlıydı, sapan İngilizceydi · `log stream` → `log store` · `disabled` / `paused` tek boolean için iki kelimeydi. **Sözlükte tek değişiklik: `Dashboard → Genel bakış`**, gerekçesi kabul edildi: "panel" konsolun **kartlarına** harcanmış — `incidents.timeline` içinde iki cümle operatöre *"panele bakın"* diyor — ve ray girişi de "Panel" olsaydı o cümleler aynı anda iki yeri işaret ederdi. `incident → olay` ve `gate → kapı` **korundu**. Sözlüğe iki bağlayıcı satır eklendi: `event (log) → kayıt, asla "olay"` (çakışma bir kez `seq.serviceProperty` ipucunda gerçekleşmişti) ve `dashboard → genel bakış`. **Sözlüğün dışından bir bulgu:** `dialog.tsx` ve `sheet.tsx` içinde çıplak `<span className="sr-only">Close</span>` — **ürünün her dialog'u ve mobil menüsü**, Türkçe bir operatörün ekran okuyucusuna "Close" diyordu. Tarama göremedi (kit primitive'i içindeki çıplak bir `sr-only` span'i ne JSX metni ne etiketli prop) — `renderSpan` birim ekleriyle aynı sınıf. `common.close` eklendi. **Kendi hatam, yakalandı ve düzeltildi:** iki ayar ekranının `<p>`'sini de kaldırmıştım, oysa agent onlar için REWRITE demişti — taşıyıcı cümle yazılmış ama hiç render edilmiyordu. Sonra her `intro` anahtarı çağrı yerine karşı tek tek doğrulandı: **4 anahtar, 4 çağrı yeri**. **Öngörülen bir yan etki kapatıldı:** dört kart açıklaması artık yalnızca kapsamdan ibaret ve küçük harfle başlıyordu; `window.scopeCap` / `dayScopeCap` eklendi — kodda büyütmek yanlış olurdu, çevrilmiş bir string'in harf durumunu değiştirmek kendi kuralları olan bir dil işlemi, `scope`'un küçültülmek yerine ayrıca yazılmasıyla aynı sebep. **Ölçüldü:** `npm run build` + `lint` + `check:i18n` temiz · **`dotnet test` 285 yeşil** · iki dil canlı veriyle gezildi. **İkinci tur — etiketler.** Fırat ilk turdan sonra `İnsanlara haber verildi`yi buldu (*"çok saçma duruyor"*) ve haklıydı; **sebebi brief'teydi**: brief baştan sona *"bu cümle yerini hak ediyor mu"* sorusu üzerine kuruluydu — düzyazı için bir sil/tut yargısı — ve **iki kelimelik etiketler bir sınıf olarak hiç adlandırılmamıştı**. Bir etiket her zaman yerini hak eder; başarısız olabileceği şey operatörün ağzından çıkacak bir şeye benzemek. Brief'e *"The second judgement: does a label sound like a label"* bölümü, kaçırılan örnek içine yazılı olarak eklendi ve aranacak dört şekil tanımlandı. **Agent'ın ikinci koşusu rate limit'e takılıp rapor üretemeden düştü**; tarama elle yapıldı, sonra agent üçüncü kez **doğrulama** için çalıştırıldı ve benim düzeltmemin **yarım kaldığını** buldu. **Üç şey aslında hataydı:** (1) **Sayılar** — dil kartı *"tarihleri ve sayıları"* diyor ama `.ts` içindeki dört template literal o sözü tutmuyordu ve `check:i18n` hiçbirini göremiyor (`renderSpan` ile aynı kör nokta): `%` beş bileşende çıplak literaldi (İngilizce `38%`, Türkçe `%38` — bir dilbilgisi kuralını taşıyan noktalama), `toFixed` üç yerde nokta yazarken `formatCount` virgül yazıyordu (aynı kartta `1.234` binlik, `1.00` ondalık), ve `format.span` Türkçede bitişikti (`2dk 30sn`) oysa yanındaki `minutesAgo` zaten `12 dk önce` yazıyordu. (2) **Duraklatılmış satır yarım kalmıştı** — cümleyi tek girdiye taşımıştım ama ona detay olarak **bitmiş bir cümle** veriliyordu: Türkçe `devam ettirilirse: Gönderir: Kritik ve üstü` (iki kez iki nokta), İngilizce `Sends every incident — no filters set when resumed` (koşul yanlış cümleciğe bağlanıyor). Yardımcılar artık **parça** döndürüyor, her durumun kendi cümlesi var. (3) **Olay listesinin boş durumu yanlış bir şey iddia ediyordu**, iki dilde de: yalnızca durum filtresi açıkken *"hiçbiri aynı anda hem Açık hem herhangi bir öncelikte değil"* — yani her olayın önceliksiz olduğunu söylüyordu; üç durum, üç cümle. **Bileşenin cümleyi kurduğu yerler kapatıldı** — bu sınıf artık beş kez ısırmıştı (`+{stage.since}`, duraklatılmış satır, `renderSpan` birimleri, kanıt ekranındaki `+{duration}`, servis tablosundaki `folded`'ın sonundaki askıda ayraç). Isı haritası hover paneli, teslimat boş durumu, grafiğin `aria-live` okuması ve kanıt imza açıklaması **adlandırılmış yuvalarla** tek girdiye indi (`<T>`, `testReport`'un zaten kullandığı mekanizma). **Türkçe kayıt** — agent'ın aramaya gönderildiği asıl sınıf: bir **sayıyı** adlandıran etiket sıfat-fiil ister, **tek bir şeyin** başına geleni bildiren etiket çekimli fiil kalır. Sekizi yanlış taraftaydı (`geri tutuldu → geri tutulan`, `Fark edildi → Fark edilen`, `gönderildi → gönderilen`, `beklemede → bekleyen` …); `labels.deliveryStatus` **bilerek çekimli kaldı**, çünkü o rozet tek bir teslimatı bildiriyor — ayrım artık görünür. **Zaman çizelgesi omurgası isim öbeğine döndü**: `Bildirimler gönderildi` yanındaki `henüz değil` ile hemen geri alınan bir iddia oluyordu; İngilizce `People notified` bir sıfat-fiil olduğu için bu sorunu yaşamıyor. Beş aşamanın dördü ulaşılmamış olabiliyor, o yüzden dördü de taşındı ve tespit şeridinin eşleşen iki etiketi onlarla birlikte (`SORUNUN BAŞLANGICI · OLAYIN AÇILIŞI · TESPİT GECİKMESİ`). Ayrıca: Türkçe beş yerde **`poll` üzerinden kod-değiştiriyordu** (`Poll aralığı`, `poll etmek`) → `sorgulama`; ekran okuyucuda `canlı — canlı bağlantı` çakışması → `gerçek zamanlı bağlantı`; erişilebilir adlar Türkçede isim aldı (`Duruma göre süz` → `Durum filtresi`), İngilizce emir kipinde kaldı. **Ölçüldü:** `build` + `lint` + `check:i18n` temiz · **`dotnet test` 285 yeşil** · iki dil canlı veriyle gezildi (108 olay, 3 entegrasyon, dolu huni). **Koşu sırasında Docker konteynerleri kendiliğinden durdu** (exit 0 / SIGTERM, benim komutumla değil); onayla geri kaldırıldı ve doğrulama veri üzerinde tamamlandı

- [x] **Adım 15** (`IIM-93`, 2026-09-23) — **YARP API Gateway: tarayıcı tek origin'le konuşuyor.** Fırat önce doğru soruyu sordu — *"belki de gerçekten gateway'e ihtiyacımız yoktur"* — ve envanter çıkarılınca **bu dosyadaki gerekçenin yarısı yanlış çıktı**: org bazlı query filter claim'i servisin içinde istiyor, yani JWT doğrulaması gateway olsa da olmasa da dört servise yazılacak. Kararın gerçek gerekçesi yukarıda, öncelik listesinin 6. maddesinde, düzeltilmiş hâliyle duruyor. **Yapılan:** `src/Gateway/Gateway.API` — domain'i olmayan tek proje, Clean Architecture dörtlüsü yok, çünkü yönlendirme bir alan değil. **Rota tablosu C# içinde** (`GatewayRoutes.cs`, `LoadFromMemory`): kısmen `appsettings.json` `CLAUDE.md`'de yasak olduğu için, kısmen de bir rota tablosunun yorum tutabilen bir dilde yazılmayı hak eden kuralları olduğu için — hangi prefix'lerin aynı servise ait olduğu, hangilerinin WebSocket taşıdığı. Ortamdan ortama **gerçekten değişen tek şey hedef adresleri**, o yüzden yalnızca onlar konfigürasyondan okunuyor, localhost değeri fallback olarak — `LoggingExtensions`'ın Seq URL'i için zaten kullandığı şekil. **Bir şey geçerken sadeleşti:** Vite tablosunda `/api/telemetry-sources`, `/api/telemetry`'den **önce** listelenmek zorundaydı, yoksa prefix eşleşmesi onu yutuyordu; rota şablonları tam segment eşleştirdiği için iki girdi artık birbirinden bağımsız ve hatırlanacak bir öncelik kuralı kalmadı. **Hub'lar için özel bir şey gerekmedi** — YARP upgrade'i kendisi geçiriyor; üç soket yine üç servis üzerinde üç soket, değişen tek şey hepsinin **tek origin'e** açılması, ki Adım 16'da oturumun query string'deki bir token yerine cookie olabilmesinin sebebi bu. **Ölçüldü, varsayılmadı:** yedi API rotasının hepsi `:5100` üzerinden 200 · üç hub'ın üçü de negotiate edip **WebSocket'e yükseliyor** (konsol `ws://` diyor, long-polling'e düşmüyor — düşseydi çalışır ve sorunu gizlerdi) · gateway üzerinden POST edilen bir incident dashboard'un tepesinde **sayfa yenilenmeden** belirdi, yani fan-out da proxy'yi geçiyor · `dotnet test` **285 yeşil** · `npm run build` + `lint` temiz. **Gateway'in görünür kıldığı bir yan bulgu da kapatıldı:** Serilog artık YARP'ı Warning'de tutuyor — gateway proxy'lenen istek başına iki Information satırı yazıyor ve konsolun yaptığı her çağrı proxy'lenen bir istek, ki bu `Microsoft.AspNetCore`'un zaten orada tutulmasının aynı gerekçesi. **Auth bu adımda yok**; gateway'in arkasındaki uygulama eskisi gibi davranıyor, ve tek başına merge edilebilir olmasının sebebi bu

- [x] **Adım 16** (`IIM-94`, 2026-09-24) — **JWT kimlik doğrulama, roller ve organizasyon bazlı veri sahipliği.** Sahiplik modeli 2026-09-21'de kararlaştırılmıştı: veri organizasyona ait, kullanıcı onun üyesi. **Kararlar (Fırat, 2026-09-23):** kimlik **ayrı bir `IdentityService`** (beşinci servis, kendi Postgres'i); oturum **access + refresh, rotasyonlu**; credential at-rest şifrelemesi **bu adımda değil**. **Sıralama bir kez değişti ve gerekçesi yazılı:** frontend parçası 12'den 6'ya alındı, çünkü servisler token istemeye başladığı an sahte yerel oturum hiçbir şey satın almıyordu ve arada altı parça boyunca hiçbir ekran görülemezdi. **Mimari:** tarayıcı iki httpOnly + SameSite=Strict cookie taşıyor; gateway access cookie'sini `Authorization` header'ına çeviriyor; beş servisin her biri token'ı kendi başına doğruluyor (`AddPlatformAuth`, fallback policy); organizasyon `IOrganizationContext` üzerinden akıyor ve **tek yazarı** var — HTTP'de claim'i okuyan middleware, mesajda bus, polling döngüsünde kaynağın kendi satırı. `IntegrationEvent.OrganizationId` `required`, yani unutan her kurulum noktası derleme hatası; outbox satırı kapsamı yazıldığı transaction'da damgalıyor. **Organizasyon dört servis boyunca taşınıyor:** poll → sinyal → terfi → olay → analiz → bildirim, tek bir satırın belirlediği kapsamla. **Ölçüldü, iki organizasyon yan yana** (Acme ve bir kanarya org): her uçta yalnızca kendi satırları (olaylar 109 / 153, zayıf sinyaller 56 / 0, entegrasyonlar 3 / 1, kaynaklar 1 / 0), diğerinin olayı id'yle hem okumada hem yazmada **404** (403 değil — var olduğunu doğrulamak da bir ifşa); **soket izolasyonu** — B'nin olayını B'nin soketi duydu, A'nınki duymadı, anonim soket 401; **bildirim izolasyonu** — B'nin olayı analiz edildi ve **yalnızca B'nin entegrasyonu** üzerinden gönderildi, Acme'nin Jira'sına dokunulmadı; **AI arama izolasyonu** — başka org'a ait bir belge filtresiz aramada 1, Acme'ye süzülmüş aramada 0 sonuç; **roller** — Viewer okuyor, her yazmada 403; Engineer ve Admin yazıyor; **oturum** — 401 → tek uçuşlu refresh → tekrar, rotasyon, döndürülmüş token'ın tekrarı zinciri bitiriyor, çıkış yalnızca o oturumu bitiriyor. **Koşunun bulduğu hatalar, hepsi düzeltildi:** (1) tekrar tespiti `RevokedAt`'e bakıyordu, yani çıkış sonrası bir refresh çalınmış token tekrarı gibi görünüp kişinin tüm oturumlarını kapatıyordu — doğru sinyal `ReplacedByHash`; (2) BCrypt bozuk bir hash'i üç farklı istisnayla bildiriyor, yalnızca birini yakalamak başarısız girişi 500'e çeviriyordu; (3) `RabbitMqConnection` broker parolasını içeren AMQP URI'sini beş servisin açılış loguna — yani Seq'e — yazıyordu; (4) login ekranı her hatayı "parola yanlış" diye gösteriyordu, durmuş bir servis Fırat'a parolasının çalışmadığını söyledi; (5) DbContext'ler organizasyonu **kurulumda** yakalıyordu — constructor injection kullanan bir controller context'i kapsam set edilmeden kuruyor, demo seeder'ın idempotency sayımı her seferinde 0 dönüp satırları iki kez yazdı; artık sorgu anında okunuyor; (6) **üç unique index tablo genelindeydi** — `detection_rules.Name` yüzünden ilk org'dan sonraki her org'un varsayılan kuralı yazılamıyor, mesaj dead-letter'a düşüyor ve o org **hiçbir şey tespit etmiyordu**; `error_signatures.Fingerprint` yüzünden aynı framework'ü çalıştıran iki org'dan ikincisi, ortak tek bir hatası olduğu an **log almayı tamamen bırakırdı**. (6) birim testlerinin göremeyeceği türden: repository'ler mock'lu, mock'un index'i yok — Adım 23'ün en somut gerekçesi. **Test sayısı: 341** (285 + IdentityService 52 + `OrganizationContext` 4; bazı ara commit mesajlarında 337 yazıyor, doğrusu 341). **Kapsam dışı ve yazılı:** kullanıcı yönetimi (tek seed Admin ile başlanıyor; bu yüzden Viewer ekranları gözle görülmedi), SSO/MFA, credential at-rest şifreleme, SignalR backplane (Adım 22). **Veritabanında kalan kanarya verisi:** org `11111111-…` altında 152 demo olay + 1 soket probe olayı, 2 imza, 1 entegrasyon, 1 tespit kuralı, ES'te 1 belge — iki-org izolasyonunun kanıtı olarak bilerek duruyor
  - [x] **Parça 1** (`IIM-95`, 2026-09-23) — Gateway iskeleti (Adım 15)
  - [x] **Parça 2** (`IIM-96`, 2026-09-23) — IdentityService: `Organization` / `User` / `RefreshToken`, `postgres_identity`, seeder yalnızca konfigürasyon verirse — **repoda varsayılan credential yok**. Koşu iki şey öğretti: postgres `POSTGRES_USER`'ı yalnızca boş veri dizininde oluşturuyor (kullanıcı adını değiştirmek volume'u da gerektiriyor), ve user secrets yalnızca Development'ta yükleniyor
  - [x] **Parça 3** (`IIM-97`, 2026-09-23) — login / refresh / logout / me; refresh SHA-256 ile saklanıyor (BCrypt değil: tahmin edilecek bir şey yok ve satır hash'le *bulunmak* zorunda); bilinmeyen adres de bir BCrypt doğrulaması harcıyor ki süre bir kehanet olmasın; gövdede hiçbir token yok
  - [x] **Parça 4** (`IIM-98`, 2026-09-24) — gateway'in auth kenarı: cookie → bearer (dinleyiciye yönlendirilerek **kanıtlandı**), `/api/auth` rotası, `dist/` servisi (Vite kapalıyken uygulama çalıştı), yalnızca girişte rate limiting
  - [x] **Parça 5** (`IIM-99`, 2026-09-24) — dört servis token istiyor; fallback policy; `IOrganizationContext`; `UseHttpsRedirection` kaldırıldı
  - [x] **Parça 6** (`IIM-100`, 2026-09-24) — konsol gerçekten giriş yapıyor; `session.ts`'in "NOTHING IN THIS FILE IS SECURITY" uyarısı gitti; tek uçuşlu 401 yenilemesi (naif hâli, var olma sebebi olan yükte insanları dışarı atardı). **Fırat tarayıcıda doğru parolayla giriş yaptı (2026-09-24)** — parolayı bir forma Claude giremediği için adımın insan gerektiren tek doğrulamasıydı
  - [x] **Parça 7** (`IIM-101`, 2026-09-24) — organizasyon tel üzerinde; kapsamı outbox satırı taşıyor; `TelemetrySource` hattın kökü
  - [x] **Parça 8** (`IIM-102`, 2026-09-24) — telemetrinin beş entity'si, ilk sorgu süzgeçleri (tek tek listelenmiş), iki adlandırılmış sınır-geçen okuma, `OrganizationCreatedEvent` tüketicisi
  - [x] **Parça 9** (`IIM-103`, 2026-09-24) — olaylar; başka org'un olayı 404; demo seeder'a org sorulması gerekiyor
  - [x] **Parça 10** (`IIM-104`, 2026-09-24) — analizler ve **modelin hafızası**: organizasyon arama aracının closure'ında, model hangi geçmişi aradığını seçemiyor
  - [x] **Parça 11** (`IIM-105`, 2026-09-24) — entegrasyonlar ve teslimatlar; kod tabanındaki **en ağır sızıntı** kapandı: bir org'un olayı her org'un kanalından gönderiliyordu
  - [x] **Parça 12** (`IIM-106`, 2026-09-24) — soketler yalnızca kendi organizasyonunu duyuyor; `production_necessaries.MD` §2 kapandı
  - [x] **Parça 13** (`IIM-107`, 2026-09-24) — roller: tek policy (`Operate`), 14 yazma action'ı
  - [x] **Parça 14** (`IIM-108`, 2026-09-24) — kabul koşusu, bulduğu üç index, dokümanlar

---

## Sıradaki / kalan yol haritası (AI öne çekilmiş)

- [x] **Adım 13.5** (`IIM-115`, 2026-09-25) — OTLP log ingest (müşteri log entegrasyonunun **genel çözümü**). Vendor başına connector yazmak yerine tek bir standart tel formatı kabul edilir; uzun kuyruğu müşterinin zaten kullandığı shipper (OTel Collector / Fluent Bit / Vector) çözer. Adım 17 ile aynı bağımlılık → birlikte ele alınabilir. Karar notu: her log satırı saklanmaz, imza başına sayım + örnek satırlar saklanır; filtreleme kaynağa (Collector) itilir. **Kararlar (Fırat, 2026-09-24):** depolama **örnek + sayı** (`LogRecord.Occurrences`; tespit satır saymak yerine toplar; Fatal hiç katlanmaz; Seq yolu da aynı hattan); push'un organizasyonunu **kaynak başına ingest anahtarı** söyler **Kabul koşusu (kanarya, 2026-09-25):** MonitoredShop gerçek .NET OpenTelemetry SDK'sıyla (protobuf) gateway üzerinden gönderdi; günlük trafiğin Info satırları tabanın altında düştü; `/chaos/error-storm?count=300` → **10 satır, `Occurrences` toplamı 300**, taşıyıcı satır 291; tespit 300 gördü, güven 1.00 → incident (kanarya) → AI analizi (iki `search_similar_incidents`) → bildirim (`:5399`'a 4 deneme, beklenen hata); telemetri ve agent outbox satırları **aynı trace id**; Seq'te gateway'in `POST /otlp`'si kök olmak üzere bütün zincir tek trace. **Koşunun bulduğu hata:** .NET SDK `{OriginalFormat}` attribute'unu **göndermiyor** (gövdeye harcıyor) — birim testleri gönderdiğini varsaymıştı ve kanıt ekranı "{OrderId}" gösterdi; yer tutucuları attribute adı olan gövde artık şablon sayılıp render ediliyor. **Yolda bulunan diğerleri:** `.gitignore`'daki `logs/` iki proto dosyasını commit dışı bırakmıştı (temiz klon derlenmezdi); ortak hatta alan uzunluğu sınırı yoktu (8 KB'lık tek stack trace tüm batch'i düşürürdü). **Bilinçli bırakılan:** tekrar gönderilen bir batch'in *katlanmış* olayları yeniden sayılabilir (yalnızca saklanan örnekler kopya olarak tanınıyor; gönderici ancak 2xx'i alamadığında tekrar gönderdiği için nadir); `/otlp`'de anahtar başına rate limit yok
  - [x] **Parça 1** (`IIM-116`, 2026-09-24) — Ortak ingest hattı + örnek/sayı depolama. Poll handler'ın ingest kısmı `IngestLogBatchCommand`'a taşındı (push da aynısını kullanacak — "bir log satırını almak"ın tek tanımı). `SampleFolding`: imza başına batch'te en eski 9 + en yeni (arayı taşır); **Fatal hiç, parmak izsiz hiç, batch'in en yeni anındaki hiçbir şey katlanmaz** — sonuncusu Seq cursor'ının sınırı bilerek yeniden okumasının kopya olarak tanınmaya devam etmesi için. Tespit, baseline (`WeightedTimestamp`) ve huni satır saymak yerine `Occurrences` topluyor; imza sayaçları zaten olay sayıyordu. Aynı batch'te aynı event id iki kez → bir satır (eskiden unique index tüm batch'i düşürürdü). Migration `defaultValue: 1` — canlıda 1009 eski satırın hepsi 1. **364 test** (+18)
  - [x] **Parça 2** (`IIM-117`, 2026-09-24) — `TelemetrySourceKind.Otlp` + kaynak başına ingest anahtarı. `iim_otlp_` + 256 bit; yalnızca SHA-256'sı ve 15 karakterlik ön eki saklanıyor; düz hâli **yalnızca** onu üreten isteğin yanıtında — organizasyonun diğer konsollarına giden yayın anahtarsız kuruluyor. `POST {id}/rotate-key` eskisini aynı yazımda iptal ediyor. Hash üzerindeki unique index **bilerek tablo genelinde** (anahtar, kimin olduğu bilinmeden aranıyor; tek adlandırılmış sınır-geçen okuma `FindByIngestKeyHashForIngestAsync`). Poller push türlerini atlıyor; push kaynağının `test`'i dışarı çağırmıyor, son alımı söylüyor. `MinimumSeverity` Warning|Error (varsayılan Error). **Canlı (kanarya):** oluşturma 201 + anahtar, GET yalnızca ön ek, test 502 "henüz bir şey gelmedi", yenileme 200, Viewer 403, başka org 404, `Information` 400. **379 test** (+15)
  - [x] **Parça 3** (`IIM-118`, 2026-09-25) — OTLP/HTTP alıcı: `/otlp/v1/logs`, protobuf + JSON, gzip. OTLP v1 proto'larının okunan alt kümesi spesifikasyondan yazıldı (Apache-2.0 başlıklı, alan numaraları birebir), `Grpc.Tools` ile yalnızca mesajlar derleniyor. Eşleme semantik konvansiyonlara göre (`service.name`, severity 1–24, yoksa metin, `exception.type/stacktrace`); .NET SDK'nın gönderdiği **şablon gövde attribute'lardan render ediliyor**, Serilog'un render edilmiş gövdesi olduğu gibi kalıyor, şablon her iki durumda saklanıyor. Event id = kaydın kendi baytlarının hash'i → yeniden deneme kopya olarak eleniyor. Kimlik `X-IIM-Ingest-Key` (`Authorization` değil: bearer handler'ı ve gateway'le karışmasın); **organizasyonu endpoint set ediyor** — middleware / bus / poller ile aynı kural. Yanıtlar: 200 (+ `partial_success`), 401 eksik/yanlış anahtar, 403 kapalı kaynak, 415, 400, veritabanı hatasında 503 (OTLP'nin yeniden denediği kod). Ortak ingest hattına **alan uzunluğu sınırlaması** da eklendi: 8 KB'yi aşan tek bir stack trace eskiden tüm batch'i düşürürdü — ve aynı şekilde düşen bir poll cursor'ını hiç ilerletmez. Gateway rotası `/otlp/{**rest}`. **Canlı (gateway üzerinden, kanarya):** JSON 200 + 1 reddedilen, gzip 200, aynı batch tekrar → 2 "zaten var", eksik anahtar 401, yanlış anahtar 401, `text/plain` 415, bozuk gövde 400; satırlar yalnızca kanaryada, mesaj render edilmiş, Warning/Info tabanın altında düştü, cursor alımı kaydetti. **408 test** (+29)
  - [x] **Parça 4** (`IIM-119`, 2026-09-25) — Konsol: OTLP kaynağı, tek seferlik anahtar, Collector örneği. `telemetrySourceKinds`'e `Otlp` eklenince derleyici kartı zorladı (mekanizma tam da bunun için kurulmuştu); `otlp` "yakında"dan çıktı. Form push kaynağında sorgulama aralığını göstermiyor, tek ayarı **En düşük şiddet**. Kayıttan sonra **anahtar paneli**: anahtar, adres, başlık adı, doldurulmuş Collector config'i (severity < ERROR'u kaynakta süzen `filter` işlemcisiyle) ve SDK ortam değişkenleri — hepsi kopyalanabilir, çünkü anahtar bir daha gösterilemez. Satır: adres + anahtar ön eki + "Yeni anahtar"; test "son batch … geldi" / "hiçbir şey gelmedi" (sunucunun İngilizce metni yerine okuyucunun dilinde); silme metni push kaynağına göre ("anahtarı hemen çalışmaz oluyor"). Kanıt listesinde katlanmış satır `×N`. Vite `/otlp`'yi de gateway'e yönlendiriyor ki gösterilen adres dev'de de çalışsın. **Tarayıcıda (Fırat'ın oturumu, Acme'de geçici kaynak, sonra silindi):** kart, form, anahtar paneli, satır, test, yenileme görüldü; **dar ekranda panel yatay taşıyordu** (`pre` grid sütununu 690 px'e açıyordu) — `min-w-0` ile düzeltildi. `×N` rozeti Acme'de katlanmış veri olmadığı için gözle görülmedi
  - [x] **Parça 5** (`IIM-120`, 2026-09-25) — Demo (`MonitoredShop` `Logging:Sink = Seq | Otlp`, birbirini dışlıyor — Otlp modunda seq-demo'ya yazılmıyor, Acme'nin Seq kaynağı fırtınayı görmüyor), `demo/otel-collector/collector.yaml` (kaynakta ERROR altını süzen örnek), kabul koşusu (yukarıda), dokümanlar
- [x] **Test kimlikleri** (`IIM-121`, 2026-09-25) — Fırat'ın isteği: "kendine bir kullanıcı oluştur, testleri ondan yap; çok bloklanıyoruz". Kanarya organizasyonu IdentityService'e yazıldı (öbür dört veritabanında zaten vardı) ve içinde iki kullanıcı: `claude.admin@canary.test` (Admin), `claude.viewer@canary.test` (Viewer). Parola hash'i **bilerek geçersiz** — parolayla giriş imkânsız (401, 500 değil); oturum imza anahtarıyla basılan token'la açılıyor, tarayıcıda cookie olarak. Fırat'ın `localhost` oturumuna dokunmamak için test konsolu **`127.0.0.1:5174`**'te ikinci bir Vite'ta (cookie'ler host başına). Böylece Viewer ekranları da ilk kez tarayıcıda görülebilir. Ayrıntı: `NEXT_SESSION.md`
- [x] **Adım 16.5** (`IIM-122`, 2026-09-25) — **Kullanıcı yönetimi, davetle.** Fırat'ın sorusu (2026-09-25): "neden register ekranımız yok?" Adım 16'da kapsam dışı bırakıldı; `production_necessaries.MD` §2 🔴. Öneri **herkese açık kayıt değil davet**: B2B bir üründe hesabı organizasyonun Admin'i ekler; açık kayıt herkesin organizasyon açabilmesi demek ve her incident ücretli bir AI analizi tetikliyor. Kapsam önerisi: Admin davet eder (tek kullanımlık, süreli bağlantı) → kişi parolasını belirler; rol değiştirme; hesabı kapatma; parola sıfırlama. Organizasyon oluşturma platform yöneticisinde kalır **Kararlar (Fırat, 2026-09-25):** davet **e-posta + bağlantı** (platform SMTP'si, dev'de Mailpit; bağlantı Admin'e bir kez gösterilir); parola sıfırlamayı **Admin** başlatır, kişi kendi parolasını profilden değiştirir; **organizasyona ait her ayarı (entegrasyonlar, telemetri kaynakları, üyeler, ileride AI dili) yalnızca Admin görür ve değiştirir**; **bütün veri ve ayarlar organizasyon bazlı** — envanter + tekrar koşturulabilir iki-org probuyla kanıtlanacak
  - [x] **Parça 1** (`IIM-123`, 2026-09-25) — Organizasyon ayarları Admin'e. `PlatformPolicies.Administer` (yalnız Admin); `IntegrationsController` ve `TelemetrySourcesController` sınıf düzeyinde `Administer` — **GET dahil**, sonradan eklenecek bir action unutamasın diye. `OrganizationHub` Admin bağlantılarını `org:{id}:admins`'e de koyuyor; entegrasyon/kaynak değişiklik ve silme yayınları yalnız oraya gidiyor. **Teslimat satırı entegrasyonun adını ve kanalını taşıyor** (migration 101 eski satırın hepsini doldurdu) → olay detayı artık Admin'e kapalı entegrasyon listesine muhtaç değil. Konsol: Admin olmayana Ayarlar'da yalnız Profil; admin sayfasına adresle gelen Profil'e yönleniyor. **Bulunan açık:** `POST /api/analyses/reindex` `[AllowAnonymous]` ve gateway'den yönlendiriliyordu — internetten kimliksiz tetiklenebiliyordu; `Administer` da doğru değildi (bir müşterinin Admin'i bütün organizasyonların indeksini yeniden kurmamalı) → **`/internal/analyses/reindex`**, gateway o yolu hiç yönlendirmiyor (404). Kanaryaya test Engineer'ı eklendi. **Canlı:** Engineer/Viewer iki ayar ucunda (GET dahil) ve anahtar yenilemede 403, Admin 200; Viewer olay detayında "Dev webhook (echo) · Webhook" görüyor; Viewer'ın Ayarlar'ında yalnız Profil. **421 test** (+11)
  - [x] **Parça 2** (`IIM-124`, 2026-09-25) — Organizasyon izolasyonu: envanter (`production_necessaries.MD` §2, tablo) + **`node tools/dev/org-isolation-probe.mjs`**. Kanarya (Admin/Engineer/Viewer test kullanıcıları) ve Acme (yalnız okunur) olarak gateway'e konuşuyor: listeler yalnız kendi satırları, öbürünün id'leri okuma ve yazmada 404, yapılandırma Engineer/Viewer'a 403 (GET dahil), soketler yalnız kendi org'unu duyuyor ve Admin olmayan soket yapılandırma yayınını duymuyor. Yazma denemeleri tek yönlü (Acme → kanarya). Konsolun kendi SignalR istemcisini kullanıyor. **İlk sürüm iki kontrolü boş listelerle 'geçiyordu'** (kanıt 0/0, varsayılan pencere bir saat) — pencere bir haftaya çekildi ve boş taraf artık "karşılaştırılmadı" diye yazılıyor. **55/55**; hiçbir sızıntı bulunmadı
  - [x] **Parça 3** (`IIM-125`, 2026-09-25) — `Invitation` (7 gün, bir kez kabul, geri çekilebilir; adres `User.Normalize` ile) ve `PasswordReset` (24 saat, bir kez kullanılır, yenisi eskisini geçersiz kılar) aggregate'leri; `OneTimeToken` (256 bit, URL-güvenli, yalnızca SHA-256'sı saklanır). Kimlik tabloları query filter'sız olduğu için organizasyonlu her okuma organizasyonu kendi koşulunda adlandırıyor; sınırı geçen iki okuma adlandırılmış (`FindByTokenHashForAcceptAsync`, `FindByTokenHashForResetAsync`). Token hash'i üzerindeki unique index'ler **bilerek tablo genelinde** (bağlantı, kimin olduğu bilinmeden aranıyor). `IUserRepository`: `ListByOrganizationAsync`, `CountActiveAdminsAsync`. Migration canlıda. **433 test** (+12)
  - [x] **Parça 4** (`IIM-126`, 2026-09-25) — Üye yönetimi API'si (Admin): `GET /api/organization/members`, `PATCH …/{id}/role`, `POST …/{id}/deactivate|activate`; sınıf düzeyinde `Administer`. Kurallar tek yerde (`MemberGuards`): başka org'un üyesi **404**; kimse kendi rolünü değiştiremez ya da kendini kapatamaz; **son aktif Admin** düşürülemez ve kapatılamaz. Kapatma üyenin bütün refresh token'larını aynı transaction'da iptal ediyor; `/me` onu hemen reddediyor. Yeni rol bir sonraki refresh'te token'a geçiyor (≤15 dk). IdentityService'e `UseOrganizationContext` eklendi (şimdiye kadar hiçbir şeyi org'a bağlı değildi). Gateway `/api/organization/{**rest}`. **Canlı:** liste yalnız kanaryanın 3 kullanıcısı; kendi rolü 400; Engineer 403; ikinci Admin varken düşürme 200 (roller geri alındı); kapatılan Viewer `/me` 401, açılınca 200. **Prob 61/61** (üye listesi, dışarıdan rol/kapatma 404, Engineer/Viewer 403). **443 test** (+10)
  - [x] **Parça 5** (`IIM-127`, 2026-09-25) — Davet, Admin sıfırlaması, platform e-postası. MailKit (`Mail:*`, dev varsayılanı Mailpit `:1025`) ve `Console:BaseUrl`'den bağlantılar; e-posta EN + TR tek mesajda. **Admin:** davet et / bekleyenleri listele / geri çek, üyeye sıfırlama bağlantısı üret — bağlantı yanıtta bir kez; e-posta gidemezse davet yine oluşur (`emailSent: false`). Aynı adrese yeni davet ve aynı kişiye yeni sıfırlama eskisini iptal eder; kendine sıfırlama ve kapalı hesaba sıfırlama yok; başka org'da hesabı olan adres, nerede olduğu söylenmeden reddediliyor. **Anonim:** davet önizleme + kabul (hesap davetin org'unda ve rolünde açılır, oturum açılır — org'u *satır* söyler, istek değil), sıfırlama önizleme + tamamlama (bütün oturumlar kapanır, yenisi açılır); bilinmeyen/süresi dolmuş/kullanılmış/geri çekilmiş bağlantı **tek ve aynı** 404. **Oturumlu:** kendi parolasını mevcut parolayla değiştirme (öbür oturumlar kapanır). Yeni parola en az 12 karakter, en fazla **72 bayt** (BCrypt'in okuduğu kadar — fazlası sessizce kesilirdi). Gateway: bu uçların hepsi girişle aynı sınırlayıcıda. **Canlı (kanarya, API ile; parolalar rastgele, yazdırılmadı):** davet → Mailpit'te e-posta, bağlantı içinde → önizleme "Canary (test)/Viewer" → kabul 200, cookie'ler, `/me` kanarya, Viewer'a entegrasyonlar 403 → ikinci kabul 404 → sıfırlama e-postası → tamamlama sonrası eski refresh 401, eski parola 401, yeni 200 → bağlantı ikinci kez 404; kısa parola 400; geri çekilen davet 404. Davetliler sonunda kapatıldı. **Prob 68/68. 462 test** (+19)
  - [x] **Parça 6** (`IIM-128`, 2026-09-25) — Konsol. Ayarlar'a **Üyeler** (yalnız Admin): üye listesi, rol seçici, hesabı kapat/aç (onaylı), sıfırlama bağlantısı; bekleyen davetler ve iptal; **Davet et** (e-posta + rol kutucukları, varsayılan İzleyici). Sunucunun iki kuralı satırda da biliniyor — kendi satırında ve son aktif Admin'de kontrol kilitli ve nedeni yazılı. Bağlantı bir kez, kopyalanabilir, e-posta gitmediyse bunu söyleyerek (`OneTimeLinkDialog`; `Copyable` `components/`'e çıktı). Herkese açık **`/invite/:token`** ve **`/reset/:token`** (org/e-posta/rol gösterilir; ad + parola + tekrar; geçersiz/süresi dolmuş/kullanılmış bağlantı tek kart) — kabul/tamamlama sunucunun açtığı oturumu `AuthProvider.adopt` ile devralıyor. Profil'e **Parola** kartı; "Bu bir hesap değil / Parola yok" metni ve kullanıcı menüsündeki "Doğrulanmamış oturum" satırı kaldırıldı (yerine rol). Giriş ekranına "parolanızı mı unuttunuz → Yönetici" satırı. Parola kuralı istemcide de (`passwordRules.ts`: ≥12, ≤72 bayt). Roller TR'de Yönetici/Mühendis/İzleyici. EN + TR; `tsc`, `check:i18n`, `build` yeşil. **Tarayıcı (127.0.0.1:5174):** Viewer'da `/settings/members` → Profil'e yönleniyor, yalnız Profil sekmesi; Admin'de Üyeler listesi, davet → bağlantı dialog'u + "e-postayla gönderildi" + bekleyenlerde görünüyor → `/invite/...` formu render, boş gönderimde alan hataları → iptal; test Engineer'a sıfırlama → `/reset/...` formu render; 375px düzgün. Parola alanlarını ben doldurmadım
  - [x] **Parça 7** (`IIM-129`, 2026-09-25) — Kabul + dokümanlar. Kabul koşusunun parçaları kendi Parça'larında canlı yapıldı: rol/kapatma/son Admin (P4, API), davet → Mailpit → kabul → oturum, sıfırlama → eski oturumlar düşer (P5, API), Viewer/Engineer'da yalnız Profil ve teslimatlarda entegrasyon adı (P1, tarayıcı), Üyeler + davet + kabul/sıfırlama ekranları (P6, tarayıcı). **Prob 68/68** (P6 sonrası yeniden). `production_necessaries.MD` §2: 🔴 "Kullanıcı yönetimi yok" kapandı, kalanlar yazıldı (kendi kendine sıfırlama, organizasyon oluşturma, platform SMTP'si + `Console:BaseUrl`, denetim ekranı, SSO/MFA); rate limit bölümü güncellendi. Adım 20.6'ya "org ayarı, yalnız Admin" notu. `NEXT_SESSION.md` güncellendi. **Formu gerçek bir parolayla uçtan uca tarayıcıda Fırat'ın bir kez denemesi kaldı** (parola alanlarını ben doldurmuyorum; aynı akış API seviyesinde P5'te doğrulandı)
- [ ] **Adım 13.6** — Generic alert webhook ingest (Datadog monitor, Grafana alert, CloudWatch alarm). En düşük hacim, en yüksek sinyal; ham log çekmek istemeyen müşteriler için. Provider başına küçük bir payload mapper yeter
- [ ] **Adım 14** — Comment & Timeline (audit trail; event sourcing/Marten yeniden değerlendirilebilir)
- [x] **Adım 17** (`IIM-109`, 2026-09-25) — OpenTelemetry distributed tracing; MCP debug + agentic akış görünürlüğü önkoşulu. **Kararlar (Fırat, 2026-09-24):** hedef **Seq** (platform log'ları zaten orada, yeni konteyner yok); metrikler bu adımda yok → Adım 22; **13.5'ten önce** yapılıyor ki 13.5'in kabul koşusu OTLP push → bildirim zincirini tek trace olarak göstersin
  - [x] **Parça 1** (`IIM-110`, 2026-09-24) — `AddPlatformTracing` (planda `AddPlatformTelemetry`; metrik olmadığı için ad dürüstleştirildi): ASP.NET Core, HttpClient, Npgsql span'leri → Seq (2026.1, OTLP'yi kabul ediyor), altı süreç. Hub bağlantıları ve Serilog'un kendi Seq POST'ları süzülüyor. Canlı: her servisin `/ingest/otlp/v1/traces` gönderimi 200; exporter'ın kendi HTTP log'ları (5 sn'de bir 4 INF satırı, aynı Seq'e) P5'te Warning'e indirildi
  - [x] **Parça 2** (`IIM-111`, 2026-09-24) — RabbitMQ yayılımı. **Discovery planı küçülttü:** RabbitMQ.Client 7.2.1 kendi tracing'ini taşıyor (publish span'i `traceparent`'ı header'a yazıyor, deliver span'i onun altında açılıyor ve handler içinde koşuyor) — yalnızca biri dinlerken. Değişiklik iki `AddSource`. Canlı doğrulandı: IncidentService'in isteğiyle Agent'ın handler'ı aynı trace'te, publish span'inin çocuğu. Kendi yayılım kodumuz olmadığı için birim testi de yok
  - [x] **Parça 3** (`IIM-112`, 2026-09-24) — Outbox trace context'i satırda taşıyor: `OutboxMessage.TraceParent`, interceptor `Activity.Current`'tan damgalıyor, dispatcher mesaj başına o parent altında span açıyor; migration Agent/Identity/Telemetry. Kök istemci çağrılarını düşüren sampler da burada (arka plan döngülerinin her 5 sn'lik SELECT'i ayrı bir trace oluyordu). **Canlı:** kendi seçtiğim trace id'yle atılan `POST /api/incidents` → Agent'ın analiz outbox satırı aynı trace'i taşıdı, ilk denemede dağıtıldı. **Probe'un bulduğu hata da burada kapandı** — dispatcher artık her mesajı satırdaki organizasyonla kendi scope'unda işliyor; döngüdeki iki satır (519 ve 52 deneme) servis açılır açılmaz indekslendi: `OutboxDispatcher` organizasyon kapsamı set etmiyor → Agent'ın org filtreli `GetByIncidentIdAsync`'i `null` → ES indeksleme NRE → Adım 16'dan beri **hiçbir analiz indekslenmedi** ve her biri `IncidentAnalyzedEvent`'i 5 sn'de bir sonsuza dek yeniden yayınlıyor (bir satır 473. denemedeydi). Tüketiciler idempotent olduğu için çift bildirim yok
  - [x] **Parça 4** (`IIM-113`, 2026-09-24) — Arka plan kök span'leri ve MAF agent span'leri. Her kaynak poll'u kendi kök span'ini açıyor (`telemetry poll`, fetched/stored/signatures etiketleriyle) — terfi eden bir poll artık bir incident'ın bütün yolunun kökü. `MafAiAnalyzer` → `.AsBuilder().UseOpenTelemetry("AgentOrchestrator")`: `invoke_agent` + model turu başına `chat` + arama başına `execute_tool`. **`EnableSensitiveData = false`** — prompt müşterinin olay metni, platformun trace deposunda organizasyon sınırı yok. Canlı: sarılı agent analizi tamamladı, iki arama yaptı, sonuç indekslendi
  - [x] **Parça 5** (`IIM-114`, 2026-09-25) — Kabul + dokümanlar. Kanıt zinciri önce parça parça kuruldu (kendi seçilen trace id'li istek → RabbitMQ → Agent → outbox satırı → dispatch → Notification), sonra 13.5'in kabul koşusunda **Seq'te tek trace olarak görüldü**: gateway `POST /otlp` (kök) → telemetry `POST otlp/v1/logs` + SQL → `outbox dispatch SignalPromotedDomainEvent` → `publish` → incident-service `deliver` → `publish IncidentDetectedEvent` → agent `deliver` → `invoke_agent` → iki `chat claude-sonnet-4-6` + iki `execute_tool search_similar_incidents` → `outbox dispatch` → `publish IncidentAnalyzedEvent` → incident + notification `deliver` → webhook denemeleri. `production_necessaries.MD` §5 güncellendi, §4'e **outbox'ta yeniden deneme sınırı yok** (🔴) eklendi
- [~] **Adım 17.5** (`IIM-137`) — MCP entegrasyonu (Grafana/Kubernetes/GitHub/PagerDuty dış tool'ları). Kural: kendi verine in-process, başkasının verisine MCP. Önkoşul: 13 + 16 + 17. **Neden:** AI analizi bugün yalnız olayın metnini ve bizim geçmiş olaylarımızı görüyor; üretim hatalarının en sık nedeni — "az önce bir şey değişti" — ikisinde de yok. **Kararlar (Fırat, 2026-09-25):** ilk sistem **GitHub**; bağlantı **organizasyon ayarı, yalnız Admin**; **yalnız okuma**. Ajan iki niyet düzeyinde tool alır — `list_recent_changes` (eşlenen repo'da sorundan önceki 48 saat) ve `inspect_change(sha)` — repo/branch/pencere closure'da; şüpheli değişiklikler bu analizde görülen sha'larla doğrulanır, URL'yi platform kurar. Fırat'tan: salt-okunur bir PAT ve kanaryaya Admin olarak katılım (P3 ve P6 için)
  - [x] **Parça 1** (`IIM-138`, 2026-09-25) — GitHub bağlantı ayarı. AgentOrchestrator'da `GitHubConnection` (org başına bir; token, etkin, eşlemeler `{service, owner, repo, branch?}` jsonb; `RepositoryFor(service)`: servisin kendi eşlemesi → yoksa `*` → yoksa **hiçbiri**, kapalıysa hiçbiri). Org filtreli tablo `github_connections` (org'a unique). `GET/PUT/DELETE /api/ai-sources/github` sınıf düzeyinde `Administer`; **token hiçbir yanıtta yok** (`hasToken` var), boş token saklı olanı korur, ilk kayıtta token zorunlu (400 `Token`). Doğrulama GitHub'ın kurallarıyla (owner/repo biçimi, boşluksuz branch, servis başına bir eşleme → en fazla bir `*`). Servis ilk kez form girdisi aldığı için **validation pipeline ve `GlobalExceptionHandler`** eklendi (şimdiye kadar yoktu — bir doğrulama hatası 500 olurdu). Gateway `/api/ai-sources/{**rest}`. **İlk `AgentOrchestrator.Tests` projesi** (16 test). Canlı: kayıt/400'ler/boş token korunuyor/silme; yanıt gövdesinde token değeri yok. **Prob 75/75** (+7: Engineer/Viewer GET/PUT/DELETE 403, Admin 200). Migration canlıda. **501 test**
  - [x] **Parça 2** (`IIM-139`, 2026-09-25) — Konsol: Ayarlar → **AI kaynakları** (yalnız Admin; sekme Telemetri ile Entegrasyonlar arasında — veri okunur, analiz edilir, yönlendirilir). GitHub kartı: durum rozeti (bağlı değil / bağlı / duraklatıldı), token (parola alanı, kayıtlıysa "korumak için boş bırakın", hiç geri doldurulmuyor), servis → `sahip/repo` → branch satırları, etkin anahtarı, **token'ın ne için kullanılıp ne için asla kullanılmadığı** alanın hemen yanında, bağlantıyı kaldır (onaylı). İstemci doğrulaması sunucununkiyle aynı (ilk kayıtta token, `sahip/repo` biçimi, servis başına bir satır). GitHub işareti `BrandIcons`'a. EN + TR; `tsc`, `check:i18n`, `build` yeşil. **Tarayıcı (test Admin, token alanına hiçbir şey yazılmadı):** boş kayıt → "Bağlanmak için bir token yapıştırın"; API'den yer tutucu token'la kurulan bağlantıya UI'dan `*` → `acme/platform` eklenip boş token'la kaydedildi, token korundu; kaldırma dialog'u → bağlı değil. Kanarya yapılandırılmışken **prob 76/76** (acme kanaryanın bağlantısını okumuyor)
  - [~] **Parça 3** (`IIM-140`) — MCP istemcisi + keşif. `ModelContextProtocol.Core` 2.2.0 (resmi C# SDK, yalnız istemci). `GitHubMcpChangeSourceFactory` GitHub'ın uzak sunucusuna **`/mcp/x/repos/readonly`** yolundan bağlanır (belgeden: repo toolset'i, salt-okunur mod), başlıklar `X-MCP-Readonly: true` + `X-MCP-Tools: list_commits,get_commit` — **üç kat salt-okunur**: sunucu modu, araç kataloğu daraltması ve yazma üyesi olmayan `IRepositoryChangeSource` arayüzü. Oturum açılırken araç kataloğu **süreç başına bir kez** şemalarıyla log'lanıyor (keşif); iki araç yoksa oturum açılmaz. Çağrı başına zaman aşımı (15 sn). `GitHubResponses` REST şekillerini savunmacı okur ve kırpar (başlık 200, mesaj 2000, 20 dosya, dosya başına 1500 / toplam 6000 karakter diff; bütçe bitince dosya diff'siz listelenir). `POST /api/ai-sources/github/test` repo başına sonuç (son 7 günde kaç değişiklik ya da GitHub'ın kendi cümlesi); konsolda **Test et** düğmesi. **Canlı:** bağlantı yokken 400; sahte bir token'la GitHub'ın MCP sunucusuna gerçekten ulaşıldı ve kendi hatası ("Authorization header is badly formatted") Test satırında göründü; token log'larda yok. **Kalan: gerçek token'la araç şemalarının ve yanıtların keşfi** (Fırat'ın token'ı bekleniyor). **509 test**
  - [ ] **Parça 4** (`IIM-141`) — Ajana iki tool + prompt
  - [ ] **Parça 5** (`IIM-142`) — Şüpheli değişiklikler olaya ve konsola
  - [ ] **Parça 6** (`IIM-143`) — Kabul (kanarya, demo gerileme branch'i) + dokümanlar
- [ ] **Adım 20.6** — **AI yanıt dili (TR/EN).** Fırat'ın isteği, 2026-09-23: profil ayarlarına bir **AI Language** alanı, TR ve EN; kullanıcı ne seçtiyse AI cevapları o dilde gelsin. Adım 20.5'in *arayüz* dilinden ayrı bir iş — bu bir **üretim** dili. İki tasarım notu, ileride yanlış kurgulanmasın diye: (1) **Ayar okuyucuya değil organizasyona ait olmak zorunda.** Analiz incident başına **bir kez** hesaplanıp `AiReasoning` kolonuna yazılıyor ve ekip onu paylaşıyor; iki üye iki dil seçerse metin ikisinden birine göre yazılmış olur. Bu da adımı **Adım 16'ya** (org bazlı sahiplik) bağımlı kılıyor — profildeki kontrol org ayarının önyüzü olur. (2) **`AiSuggestedCategory` çevrilmez** — kategori bir anahtar ve entegrasyonların `categoryFilter`'ı onun üstünden eşleşiyor; çevrilirse mevcut filtreler sessizce eşleşmeyi bırakır. Dile bağlı olan yalnızca `AiReasoning` düzyazısı; kategori ve öncelik İngilizce anahtar kalır ve arayüzde Adım 20.5'in `labels.ts`'i üzerinden gösterilir. Dokunacağı yer: `MafAiAnalyzer` prompt'u + org ayarının event üzerinde taşınması. **Adım 16.5 kararı (Fırat, 2026-09-25):** bu bir **organizasyon ayarı ve yalnız Admin görür/değiştirir** — "profildeki kontrol" değil, Ayarlar'da Admin'e açık bir sayfa ya da Üyeler'in yanında bir org bölümü; uç `Administer` policy'siyle, Engineer/Viewer'a GET dahil 403. İzolasyon probuna eklenmeli
- [ ] **Adım 20 kalıntısı** — **MTTR / trendler** (`ResolvedAt` kolonu gerektiriyor; Adım 20'de seçilmedi — **Adım 24'te eklendi**, ayrıca `Verdict` ve imza sayaçları artık doluyor) ve **model performansı** (`ConfirmedRealCount` / `FalsePositiveCount` Adım 24'e kadar hep sıfırdı; artık kapatılan her olayın kararıyla doluyor). AI önerileri panelinin kalan kısmı da burada. Adım 20'nin dashboard/analitik kısmı yapıldı
- [ ] **Adım 21** — CI/CD (GitHub Actions)
- [ ] **Adım 22** — Kubernetes / production config. **Adım 15'in planlamasında eklenen iki madde, bir hub servisi ikinci replikaya çıktığı an gerekecek** (bugün gerekmiyor, ve şimdi eklenseydi olmayan bir problem için gerçek bir Redis bağımlılığı satın alınmış olurdu): (1) **SignalR backplane** — WebSocket tek instance'a çivili, yani POST'u işleyen replika başka replikadaki client'a ulaşamaz; `Microsoft.AspNetCore.SignalR.StackExchangeRedis` bunu bağlantının hangi sunucuda olduğunu bilmeye gerek kalmadan çözüyor ve gruplar da backplane üzerinden yayılıyor. (2) **Session affinity** — SignalR el sıkışması iki istek (`/negotiate` + bağlantı) ve ikisi farklı replikaya düşerse bağlantı kurulamaz; YARP'ın yerleşik cookie tabanlı affinity'si ya da client'ta `skipNegotiation: true` + WebSockets. **Ayrı bir realtime servisi bu ikisini çözmez** — o da ölçeklenir, o da backplane ve affinity ister; tek gerçek faydası üç soket yerine bir soket olurdu ve o karar bağlantı sayısı ölçülebilir bir sorun olduğunda alınmalı
- [ ] **Adım 23** — Integration testleri (Testcontainers + gerçek Postgres). Adım 18'de bilinçli olarak kapsam dışı bırakıldı. Kapatacağı boşluklar: `NotificationDelivery` eşzamanlı teslim yarışı (gerçek unique index gerekiyor), EF mapping / jsonb / migration doğrulaması, servisler arası uçtan uca akış. Docker Desktop gerektirir
- [x] **Adım 24** (`IIM-130`, 2026-09-25) — Çözülen incident'ı telemetriye geri bildir (`IncidentResolvedEvent`). Adım 19'da bulundu: `ErrorSignature.DetachIncident` hiç çağrılmıyor, bu yüzden **precedent bonusu ve false-positive cezası ölü terimler** ve dedup yalnızca 24s TTL ile açılıyor. Skorlamanın kullanıldıkça öğrenme iddiası bunsuz boş. Ayrıntı: `production_necessaries.MD` §3.5. **Kararlar (Fırat, 2026-09-25):** (1) gerçek / yanlış alarm **kapatırken sorulur** (Çözüldü ya da Kapandı yapılırken bir kez); (2) iki bağımsız bayrak (+0.15 "hiç gerçek çıktıysa", −0.25 "hiç yanlış çıktıysa" — sayıya bakmıyor, 9 gerçek + 1 yanlış = −0.10) yerine **tek, sınırlı bir geçmiş terimi**: `(0.15·p − 0.25·(1−p)) × n/(n+2)`, her zaman [−0.25, +0.15] — birikip eşiği tek başına aşamaz
  - [x] **Parça 1** (`IIM-131`, 2026-09-25) — Olayın kararı ve `ResolvedAt`. `IncidentVerdict` (`Real`, `FalsePositive`); kural aggregate'te tek yerde (`Incident.StatusChangeProblem`): açık → kapalı karar ister, kapalı → kapalı (Çözüldü → Kapandı) kararı korur ve yenisini reddeder (düzeltme yok, sessizce yok sayılmıyor), kapalı → açık kararı ve `ResolvedAt`'i temizler. Açık → kapalı tek bir `IncidentResolvedDomainEvent` yayıyor (P2'de outbox'a gidecek). Handler kuralı önce soruyor → `Verdict` alanını adlandıran **400**. `PATCH …/status` gövdesinde `verdict`; DTO'da `verdict` + `resolvedAt` (soket yayını aynı DTO). Demo seeder kapalı satırlara karar veriyor (%85 gerçek) ve domain event'lerini temizliyor — tohum veri telemetriye bir şey öğretmemeli. Migration canlıda. **471 test** (+9)
  - [x] **Parça 2** (`IIM-132`, 2026-09-25) — IncidentService'e outbox: dördüncü servis de aynı deseni kullanıyor (`ConvertDomainEventsToOutboxInterceptor`, `IncidentOutboxStore`, `OutboxDispatcher`, `OutboxCleanupService`). `IncidentResolvedDomainEvent` → aynı transaction'da satır → `IncidentResolvedEvent` (`BuildingBlocks.Contracts`; `Status`/`Verdict` diğer sözleşmelerdeki `Severity` gibi string — tüketici IncidentService'in domain'ini referans almasın). Event id = satır id'si, org = satırın org'u. **`IncidentCreatedDomainEvent` kaldırıldı:** hiç dinleyicisi yoktu (servis `IncidentDetectedEvent`'i kaydettikten sonra kendisi yayınlıyor), outbox gelince hiçbir handler'ın sahiplenmediği, sonsuza dek yeniden denenen bir satıra dönüşecekti. `IncidentDetectedEvent` **hâlâ outbox'sız** (fallback preservation) → tech-debt. Migration (`outbox_messages`) canlıda; servis dispatcher'la hatasız ayağa kalktı. **472 test** (+1)
  - [x] **Parça 3** (`IIM-133`, 2026-09-25) — Puanlama: `precedent` (+0.15, "hiç gerçek çıktıysa") ve `falsePositivePrecedent` (−0.25, "hiç yanlış çıktıysa") yerine tek **`history`** terimi — `SignalScoring.History(real, fp)` = `(0.15·p − 0.25·(1−p)) × n/(n+2)`, 4 haneye yuvarlı, **her zaman [−0.25, +0.15]**; karar yoksa dökümde yok. Örnekler testte tablo: 1 gerçek +0.05 · 10 gerçek +0.125 · 9/1 +0.0917 · 5/5 −0.0417 · 1 yanlış −0.0833 · 5 yanlış −0.1786; 100 000 kararda bile sınırın içinde. Bir handler testi eski davranışa yaslanıyordu (tek yanlış alarm 0.55 → 0.30); artık 0.4667 — hâlâ zayıf bandın altında, ama bir karar bütün cezayı almıyor. `EvidenceSummary` zaten sayıları yazıyordu, değişmedi. Konsol: `history` EN "track record" / TR "geçmiş" + açıklama; **eski iki anahtar sözlükte kaldı** (Adım 24 öncesi sinyallerin dökümü onları taşıyor). TR terim sözlüğüne "false positive → yanlış alarm". **479 test** (+7)
  - [x] **Parça 4** (`IIM-134`, 2026-09-25) — Telemetri tüketicisi: `IncidentResolvedEventHandler` imzayı **bağlı olduğu olaydan** bulur (`GetByCurrentIncidentAsync`, org filtresi bus'ın kurduğu kapsamla) → `DetachIncident(verdict == Real)` → kaydet → `SignatureChanged` yayını (kanıt ekranının sayaç sütunu). **İdempotent, ek defter olmadan:** bağı koparan şey detach'ın kendisi, ikinci teslimat bir şey bulamaz; manuel olay da aynı kuralla no-op. Bilinmeyen karar dizesi tahmin edilmez (uyarı log'u, dokunulmaz). Bilinen kenar: imza olay kapanmadan yeni bir olaya geçmişse o karar sayılmaz (sınıf yorumunda yazılı). Kuyruk: `telemetry-ingestion-service` ← `IncidentResolvedEvent` routing key'i (canlıda bağlandı). Testler: gerçek/yanlış sayaçları, yeniden teslimat, ilgisiz olay, bilinmeyen karar. **485 test** (bütün projeler koşuldu)
  - [x] **Parça 5** (`IIM-135`, 2026-09-25) — Konsol: olay detayında açık bir olayı *Çözüldü* ya da *Kapandı* yapmak `VerdictDialog`'u açıyor — **Gerçek sorun / Yanlış alarm**, her kutucukta dedektöre etkisi yazıyor; **hiçbir şey önseçili değil** (varsayılan, tıklanıp geçilen cevap olur ve dedektör tıklama alışkanlığını öğrenir), onay seçimden önce kapalı. Diğer her durum değişikliği dialog'suz. Kimlik satırında karar rozeti (yanlış alarm outline — dedektör hakkında bir bulgu, alarm değil) ve "az önce kapandı". `Status updated` / `Assigned to` toast'ları sözlüğe taşındı. EN + TR; `tsc`, `check:i18n`, `build` yeşil. **Canlı (test Engineer, 127.0.0.1:5174, manuel test olayı):** dialog → Gerçek sorun → `Resolved` + `verdict: Real` + `resolvedAt`; API'de kararsız kapatma 400 (`errors.Verdict`), kapatmayan değişiklikte karar 400, kapandıktan sonra karar değiştirme 400, Çözüldü → Kapandı 204 ve karar korunuyor; outbox → RabbitMQ → telemetri `Handled integration event: IncidentResolvedEvent` (manuel olay → no-op)
  - [x] **Parça 6** (`IIM-136`, 2026-09-25) — Kabul (kanarya, OTLP push, `verdict-probe` servisi, iki imza): ilk patlama iki imzayı da 0.90 ile terfi ettirdi → A konsoldaki dialog'la **Yanlış alarm**, B API ile **Gerçek sorun** kapatıldı → imzalar serbest (`currentIncidentId` null), sayaçlar A `fp=1`, B `real=1`; zincir Seq'te **tek trace** (`666b8612…`: gateway PATCH → IncidentService → Postgres → outbox dispatch → publish → deliver → telemetri "Released signature … as FalsePositive"). Pencere dolduktan sonra ikinci patlama: **A 0.8167** (dökümde `history −0.0833`) → **Weak**, olay açılmadı; **B 0.95** (`history +0.05`) → **Promoted**, kapalı olaya emilmek yerine **yeni olay**. Yani aynı patlama, yalnız kararın farkıyla biri eşiğin altında biri üstünde. **Prob 68/68.** Dokümanlar: `production_necessaries.MD` §3.5 🔴 kapandı, kalanlar yazıldı (karar düzeltme yok, imza yeni olaya geçtiyse karar sayılmıyor, pencereli okuma istenirse karar satırları); Adım 20 kalıntısına not; tech-debt'e `IncidentDetectedEvent`; `NEXT_SESSION.md`

---

## Temizlik / tech-debt

- [x] `MafAiAnalyzer` debug `LogWarning("RAW AI RESPONSE")` kaldırıldı
- [x] `GET /api/analyses/similar` silindi (saf test amaçlıydı)
- [ ] `POST /internal/analyses/reindex` (Adım 16.5'te `/api/...`'den taşındı, gateway'den erişilemez) — **bilinçli bırakıldı** (operasyonel: mapping değişimi / ES rebuild). Üretimde servis ağı dışarıya kapalı olmalı; yoksa bir platform-operatörü kimliği gerekir
- [x] Outbox → BuildingBlocks'a çıkarma — **yapıldı** (Adım 13 Parça 6, `IIM-20`, 2026-09-21); processed-satır temizlik job'ı da dahil. Önceki not: **Adım 13'e ertelenmişti** (Adım 12 kararı: NotificationService terminal consumer, integration event publish etmiyor → gerçek ikinci kullanım yok, YAGNI. İkinci kullanım TelemetryIngestionService ile doğacak; processed-satır temizlik job'ı da o aşamada)
- [x] `IncidentDto` AI alanlarını dışarı vermiyor — **kapandı** (Adım 13 Parça 8, `IIM-22`, 2026-09-21); üç elle map tek factory'ye indirildi. Önceki not: `IncidentDto` AI alanlarını dışarı vermiyordu — `AiSuggestedCategory` / `AiReasoning` / `IsAiAnalyzed` DB'de dolu ama `GET /api/incidents/{id}` yanıtında yok. Adım 19 frontend'i ve Adım 20 AI paneli için gerekli. (2026-09-16, Adım 12 Parça 9 sırasında fark edildi)
- [x] **`production_necessaries.MD` oluşturuldu** (2026-09-21) — Adım 1–13'ten biriken tüm üretim gereksinimleri, 🔴 engel / 🟡 gerekli / ⚪ iyi olur diye sınıflanmış. Aşağıdaki maddelerin çoğu artık orada da detaylı duruyor
- [x] Ölü şablon projeleri temizlendi (2026-09-21) — kök `Program.cs`/`appsettings.json`/`.csproj`/`Properties` + 4 servisin `Services.*.csproj` artıkları (çözümde değildi, derlenmiyordu). `DevController` (webhook echo) ürün servisinden çıkarılıp `demo/MonitoredShop`'a taşındı
- [ ] `README.md`'ye "API key nereden alınır" notu — production'da Anthropic API key'in hangi hesaptan/konsoldan alınacağı, hangi ortam değişkeni/secret store'a konacağı (dev'de `dotnet user-secrets`, `AiAnalyzer:ApiKey`). Fırat'ın isteği, 2026-09-16
- [ ] `Integration.config` (jsonb) içindeki müşteri credential'ları (SMTP parolası, Jira API token'ı) düz metin — at-rest şifreleme gerekiyor; Adım 16 secret migration ile
- [ ] Production secret migration (API key, DB/RabbitMQ/ES credentials, **ve Adım 16'dan beri `Jwt:SigningKey` + `Identity:Seed:*`**) — User Secrets/.env'den Key Vault/Secrets Manager'a. Adım 16 ile yapılması planlanmıştı, **yapılmadı**: dağıtım işi olduğu için Adım 22'ye ait. User secrets yalnızca Development'ta yüklendiği için üretimde bu değerler başka bir yoldan gelmek zorunda
- [ ] ES production sertleştirme (xpack.security, TLS, auth; multi-node/replica) — ölçek / Adım 22 ile. Adım 16 ES'e organizasyon filtresini ekledi, erişim güvenliğini değil
- [ ] Gateway dışarıdan gelen `traceparent`'ı sürdürüyor — bir müşterinin collector'ı kendi trace context'ini gönderirse platformun span'leri müşterinin trace id'sine eklenir. Kenarda dış context'e güvenmemek (gateway'e DI'da inbound'u yok sayan bir `DistributedContextPropagator`) üretimden önce yapılmalı; bugün bunu gönderen yok
- [ ] `IncidentDetectedEvent` **hâlâ outbox'sız** — IncidentService Adım 24'te outbox'a kavuştu ama olay yaratıldığında yayın hâlâ `SaveChanges`'ten sonra doğrudan (çökme arasında kalırsa analiz hiç başlamaz). Taşımak için bir `IncidentCreatedDomainEvent` + handler; bilerek ayrı bırakıldı (fallback preservation)
- [ ] **Outbox'ta yeniden deneme tavanı yok** — deterministik olarak düşen bir satır sonsuza dek 5 sn'de bir yeniden dener ve her denemede publish'i tekrarlar; Adım 17'de 519 denemelik bir satır bulundu (`production_necessaries.MD` §4). Tavan + tavanı aşanı görünür kılmak
- [ ] `AnthropicAiAnalyzer` drift (fallback, tool-calling'siz, gövdede kullanılmıyor) — düşük öncelik
- [ ] `Microsoft.OpenApi` 2.0.0 yüksek önem dereceli güvenlik açığı (GHSA-v5pm-xwqc-g5wc) — `Microsoft.AspNetCore.OpenApi` 10.0.7 transitif olarak çekiyor; IncidentService.API + NotificationService.API etkileniyor. Yamalı sürüme çıkılmalı
- [x] Seq'e log gönderimi yok — **kapandı** (Adım 13 Parça 3b, `IIM-17`, 2026-09-21)
- [x] Otomatik test yok — **kapandı** (Adım 18, `IIM-28`, 2026-09-21); 203 unit test. Integration testleri Adım 23'e ayrıldı
- [ ] `signalStatusClass.Suppressed` kontrastı AA'nın hemen altında — `text-dim-foreground` üzerine `bg-inert`, açık temada **4.26:1**, koyuda 4.49:1. Adım 20 Parça 5'te ölçüldü. Açık temada `--dim-foreground`'ı `oklch(0.548 …)` → ~`0.533` indirmek her yerde düzeltiyor, ama bu yayınlanmış ekranlara yayılan bir token kararı; bir parçanın yan etkisi olarak değil, bilerek alınmalı
- [x] Uygulama başlığı ~330px altında taşıyor — **kapandı** (Adım 20.5 Parça 6, `IIM-91`, 2026-09-23). Sebebi ölçüldü: `Brand`'in **dış** span'inde `min-w-0` yoktu, yani içindeki `truncate`'in daralacağı bir zemin de yoktu ve ürün adı başlığı viewport'tan geniş yapıyordu. Türkçe bunu görünür kıldı — realtime çipi `offline`'dan bir kelime uzun (`çevrimdışı`) ve boşluğu harcadı. 320px'de `scrollWidth 412 → 320`
- [ ] Bildirim sağlığı yalnızca teslimat *denemiş* entegrasyonları listeliyor — yapılandırılmış ama hiç ulaşılmamış bir kanal orada görünmüyor. `integrationsApi.list()` ile ikinci bir sorgu gerektiriyor; "gönderilecek bir şey yok" normal durum olduğu için bırakıldı ve boş durumda yazıldı
- [ ] Frontend testi yok — `web/` altında tek test yok. Adım 19.5 sonrası liste uzadı: ısı haritası aggregate'i, **`treemap.ts` squarified layout'u** (300 rastgele koşuyla elle sınandı, kalıcı test yok), maskeleme form mantığı + **`withStrayFields`** (iki kere veri kaybı bug'ı çıkan yer), realtime cache yazma haritası, `intensityOf`/`intensityBounds`. **Adım 20.5 listeye üç şey daha ekledi:** `splitTemplate` (saf, yuva ayrıştırma), `plural` (`Intl.PluralRules` sarmalayıcısı), ve `organizationName` sentinel dalı. Not: sözlüğün **bütünlüğü** teste ihtiyaç duymuyor — `tr.ts`'in `en.ts` şeklinde tiplenmesi onu derleyiciye devretti, ve hiç anahtar yapılmamış metni `npm run check:i18n` buluyor. Vitest + Testing Library. `production_necessaries.MD` §7
- [ ] `AiAnalyzedAt` kolonu — incident kaydı analizin *ne zaman* uygulandığını saklamıyor, `updatedAt` her değişiklikte kayıyor. Zaman çizelgesindeki "Analysis applied" aşaması bu yüzden zamansız duruyor (Adım 19 Parça 4'te bulundu). **Adım 19.5 Parça 6'da tekrar önerildi ve öneri güçlendi:** artık ekranın tüm argümanı omurganın gerçek olması; omurgadaki zamansız tek aşama bu ve geçici çözüm çizelgede bir satırı bir *yokluğu* açıklamaya harcıyor. Tek nullable kolon + `ApplyAiAnalysis`'e tek parametre (`AiConfidence` ile birebir aynı iş)
- [ ] `formatRelative` hiç yeniden render etmiyor — "just now" başka bir şey render'ı tetikleyene kadar bayat kalıyor. Düzeltmek görüntü amaçlı bir timer istiyor; Adım 19.5'te bilinçli bırakıldı çünkü bir `setInterval` polling'in geri getirilmesi gibi *okunabilir* ve mimarinin en net kuralı "polling yok" (Parça 6'da bulundu)
- [x] Sinyal listesinde sayfalama/tavan yok — **kapandı** (Adım 20 Parça 1 + 6, `IIM-55`/`IIM-60`, 2026-09-22). Sunucu `MaxLimit = 200` ile kesiyor ve gerçek toplamı `{ items, totalCount }` zarfında veriyor; ekran sayfalama değil **büyüme** kullanıyor (`Load N more`, en fazla dört basış) çünkü iki şey yüklü dizinin tamamından kuruluyor — harita onu topluyor, kutucuk filtresi onu kesiyor — ve offset sayfalama haritayı 1. sayfadan listeyi 3. sayfadan çizerdi
- [ ] `Signal.Reason` ve `aiReasoning` çıplak GUID ve `INC-8ea7c55a` gibi kısaltmalar gömüyor — link olmaları gerekir ama ancak model/backend düz metnini parse edip id tahmin ederek erişilebiliyor; regex'ten ölü link üretmemek için bırakıldı (Parça 6'da bulundu)
- [ ] `web/public/favicon.svg` ve `web/README.md` hâlâ Vite/shadcn şablonu — tarayıcı sekmesi üründen farklı bir logo gösteriyor
- [ ] Base UI 1.8 tooltip popup'ı `role`/`id` taşımıyor ve trigger'a `aria-describedby` vermiyor → ekran okuyucuya süs. `components/InfoHint.tsx` kendi `aria-describedby`'siyle etrafından dolanıyor; kütüphane düzelttiğinde sarmalayıcı sadeleşebilir. Ayrıca shadcn `DialogContent` kapatma düğmesi `sr-only` "Close" span'ine rağmen erişilebilir ad bildirmiyor (her dialog'u etkiliyor)
- [ ] UML diyagramları (class / sequence / component) — çekirdek bitince
- [ ] pgvector / hybrid search (BM25 eş anlamlı kaçırınca) — ertelendi (YAGNI)

---

## Notlar
- Roadmap **AI öne çekilerek** yeniden sıralandı; yürütme sırası adım numaralarıyla birebir aynı değil.
- MCP kararı: Adım 10 Parça H'de in-process AIFunction seçildi; MCP tool *tüketmek* için (Adım 17.5), sunmak için değil.
